---
title: Time Domain Near Memory Computing Engine
title_zh: 时域近内存计算引擎
authors: "Sarthak Antal, Steve Enosh"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.14162v1"
tags: ["query:cim"]
score: 9.0
evidence: 时域近存计算用于低精度MAC
tldr: AI工作负载的数据移动能耗远超计算，现有模拟存内计算虽能效高但依赖ADC/DAC增加开销。本文提出一种时域近存计算架构，将数字权重通过电流舵DAC转换，输入向量经N脉冲发生器编码，在时域完成乘法运算。研究了基于延迟单元和计数器两种累加方案，并分析线性度、可扩展性等权衡。基于4x4 MAC原型，在1V电源下实现40MHz频率、42uW功耗，能效达7.62 TOPS/W。该工作平衡了数字友好设计与模拟域能效，为低精度AI加速提供新方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.14162v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 925, \"height\": 479, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.14162v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 938, \"height\": 503, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.14162v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1676, \"height\": 912, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.14162v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 948, \"height\": 395, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.14162v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 919, \"height\": 549, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.14162v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 866, \"height\": 313, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.14162v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 840, \"height\": 443, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.14162v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 694, \"height\": 522, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.14162v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 688, \"height\": 515, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.14162v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1880, \"height\": 573, \"label\": \"Table\"}]"
motivation: 数据移动能耗高，模拟存内计算模数转换开销大，亟需数字友好且高效的低精度MAC方案。
method: 权重经电流舵DAC，输入由N脉冲发生器时域编码，乘法在时域完成；累加采用延迟单元或计数器结构。
result: 实现4x4 MAC原型，供电1V，工作频率40MHz，总功耗42μW，能效达7.62 TOPS/W。
conclusion: 提出时域近存计算架构，在保持数字接口的同时实现高能效低精度MAC，为AI加速提供新思路。
---

## 摘要
AI工作负载的计算需求不断增长，使得对能效型内存内和近内存计算架构的需求更加迫切，因为数据移动消耗的能量通常远高于计算本身。虽然全数字架构提供了强大的可扩展性并支持更高分辨率的计算，但模拟内存内计算已在低精度工作负载中展现出更高的能效。然而，其对外围DAC和ADC的依赖引入了额外的功耗、面积和设计开销。为了解决这些挑战，本文提出了一种用于低精度乘累加（MAC）运算的时域近内存计算架构。在所提出的方法中，存储在SRAM中的数字权重位通过电流舵DAC进行转换，而数字输入向量由N脉冲发生器编码。这使得乘法可以在时域中执行，同时保持数字友好的接口。研究了两种累加方案——基于延迟单元的架构和基于计数器的架构，并在设计权衡、线性度、可扩展性和能效方面进行了比较。为了提高技术可移植性，N脉冲发生器和计数器采用RTL综合实现，而电流舵DAC保持在模拟域。采用1V电源实现了4x4 MAC原型，工作频率为40 MHz，功耗为42 uW，能效为7.62 TOPS/W。

## Abstract
The increasing computational demand of AI workloads has intensified the need for energy-efficient in-memory and near-memory computing architectures, particularly because data movement often consumes significantly more energy than computation itself. While fully digital architectures provide robust scalability and support higher-resolution computation, analog in-memory computing has demonstrated improved energy efficiency for low-precision workloads. However, its reliance on peripheral DACs and ADCs introduces additional power, area, and design overhead. To address these challenges, this work presents a time-domain near-memory computing architecture for low-precision multiply-and-accumulate (MAC) operations. In the proposed approach, digital weight bits stored in SRAM are converted using a current-steering DAC, while the digital input vector is encoded by an N-pulse generator. This enables multiplication to be performed in the time domain while maintaining a digital-friendly interface. Two accumulation schemes, a delay-cell-based architecture and a counter-based architecture, are investigated and compared in terms of design trade-offs, linearity, scalability, and power efficiency. To improve technology portability, the N-pulse generator and counters are implemented using RTL synthesis, while the current-steering DAC remains in the analog domain. A 4 x 4 MAC prototype is implemented with a 1 V supply, achieving an operating frequency of 40 MHz, power consumption of 42 uW, and energy efficiency of 7.62 TOPS/W.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将对您提供的论文《Time Domain Near Memory Computing Engine》进行结构化、深入、客观的中文总结。

---

### 1. 论文的核心问题与整体含义

*   **研究动机**：AI工作负载的计算需求激增，但传统架构中“数据移动”的能耗远高于“计算本身”。内存内和近内存计算被视为潜在的解决方案。
*   **核心矛盾**：
    *   **全数字架构**：鲁棒性好、支持高精度，但在低精度（如<8位）场景下能效不足。
    *   **模拟存内计算**：在低精度场景能效高，但严重依赖外围的数模转换器（DAC）和模数转换器（ADC），这会引入巨大的功耗、面积和设计开销，限制了其优势。
*   **研究意义**：本文旨在提出一种新的架构，以**平衡数字域的友好性和模拟域的高能效**，避免传统模拟存内计算中ADC/DAC的高昂代价。

### 2. 论文提出的方法论

*   **核心思想**：采用**时域计算**范式，将乘法运算中涉及的数字权重和输入向量分别转换为模拟电流和时间脉冲宽度，在时域实现乘法，并通过不同方式（延迟链或计数器）完成累加。
*   **关键技术细节**：
    1.  **乘法实现**：
        *   将**SRAM中的数字权重**通过一个**电流舵DAC (CSDAC)** 转换为模拟电流（\( I_{DAC} \)）。
        *   将**数字输入向量**通过一个**N脉冲发生器**（RTL综合实现）转换为一系列脉冲。脉冲的数量正比于输入码值，即脉冲总时长 \( T_{pulse} \propto N_{in} \)。
        *   用电流 \( I_{DAC} \) 在时间 \( T_{pulse} \) 内对一个电容 \( C \) 充电，产生的电压 \( V_{MAC} \) 即为乘法结果。公式为 \( V_{MAC} = I_{DAC} \cdot T_{pulse} / C \)。
    2.  **累加实现（两种架构）**：
        *   **架构一：基于延迟单元 (Delay-Cell-Based)**：将每个乘法结果电压 \( V_{MAC} \) 作为控制电压，去“电流饥饿”一个PMOS管，从而改变后续反相器链的传播延迟。多个这样的延迟单元级联，输出信号的累积延迟就代表累加结果。
        *   **架构二：基于计数器 (Counter-Based)**：每个乘法单元都配有一个独立的延迟单元。依次测量每个延迟单元的延迟，并将其转换为数字计数值（通过一个计数器），然后全部累加起来作为最终结果。
    *   **公式**
        *   乘法： \( V_{MAC} = I_{DAC} \cdot T_{pulse} / C \)
        *   延迟单元延迟： \( t_d \approx C_L V_{sw} / I_{starve}(V_{MAC}) \)
        *   累加量化误差：理想ADC，SNR = 6.02×ENOB + 1.76 dB；计数器量化噪声方差： \( \sigma^2_{q,t} = T_{clk}^2/12 \)

### 3. 实验设计

*   **数据集/场景**：论文并未在真实AI数据集（如ImageNet, CIFAR等）上进行端到端测试。其验证方式是基于电路仿真，评估MAC宏单元本身的性能。
*   **Benchmark**：论文通过与近期的多项相关工作进行比较来展示其性能，包括：
    *   ISCAS‘25, ISCAS’22, ISSCC‘16, ASSCC’16, ISSC‘20 等工作。
    *   对比维度为**工艺、电源电压、工作频率、功耗、能效(TOPS/W)**。
*   **对比方法**：
    *   自己提出的两种架构（延迟单元型 vs. 计数器型）进行内部对比。
    *   与文献中的其他时域/电荷域近内存/内存内计算加速器进行外部对比。

### 4. 资源与算力

*   **文中未明确提及**。本研究属于模拟/数字混合信号电路设计与仿真，其“算力”开销是**EDA工具运行仿真（如SPICE、数字综合工具）** 的时间和计算资源。论文没有提到任何GPU型号、数量或训练时长。

### 5. 实验数量与充分性

*   **实验数量**：相对有限。主要是：
    *   对两种MAC架构的核心模块（DAC、N脉冲发生器、延迟单元）进行了独立的功能仿真。
    *   对两种累加架构的**线性度**进行了对比仿真（通过Python后处理分析）。
    *   对整个4x4 MAC宏单元进行了顶层性能评估（功耗、频率等）。
*   **充分性与公平性**：
    *   **优点**：线性度对比实验清晰地展示了计数器架构的优势。通过与多篇经典论文的对比，展示了其能效指标的竞争力。
    *   **不足**：
        1.  **验证范围狭窄**：缺乏在真实神经网络应用场景下的完整系统级仿真，例如在图像分类任务上的准确率测试。
        2.  **缺乏消融实验**：没有深入研究设计参数（如电容大小、计数器时钟频率）变化对最终性能（精度、功耗）的具体影响。
        3.  **仅为仿真结果**：所有结果均来自仿真实，没有流片（tape-out）测试。仿真环境下的结果可能过于理想。
        4.  **对比不够公平**：论文与不同工艺、不同工作、不同应用场景的文献进行直接对比，缺乏一个公平、统一的Benchmark。

### 6. 论文的主要结论与发现

1.  提出了**时域近内存计算**作为一种在数字友好性和高能效之间取得平衡的可行方案。
2.  在两种累加架构中，**计数器架构在可扩展性和线性度方面优于延迟单元架构**，因为它避免了非线性误差的累积，但代价是更高的功耗和更低的吞吐率。
3.  论文**最终选择并报告了延迟单元型架构**的性能，认为其在平衡速度、能耗和控制复杂度方面更优，但线性度是其主要瓶颈。
4.  实现的4x4 MAC原型在1V电源下，实现了**7.62 TOPS/W的高能效**，证明了该架构在低精度计算中的潜力。

### 7. 优点

*   **创新性**：采用时域编码（脉冲宽度/计数）来代替传统的电压幅度域计算，有效避免了高开销的ADC。实现了“数字友好”的接口，输入输出几乎全部数字化。
*   **架构灵活性**：提出了两种不同的累加架构，并对其权衡进行了清晰的数学和仿真分析，为后续研究提供了设计空间探索。
*   **可移植性**：关键的N脉冲发生器和计数器采用RTL综合实现，具有良好的工艺可移植性，而敏感的模拟部分（DAC）被限制在最小范围内。
*   **能效指标优秀**：7.62 TOPS/W的能效在与先前工作的对比中具有竞争力，特别是在同属于仿真结果的比较中。

### 8. 不足与局限

*   **精度限制**：架构目前仅支持**4位精度**，这对于许多实际AI应用（通常要求8位或更高）是不足的。论文结尾明确指出这是未来工作方向。
*   **线性度问题**：所选的延迟单元型架构存在严重的线性度问题，随着MAC矩阵规模的增大，误差会迅速累积。这直接限制了其可扩展性和实际应用场景。
*   **仿真实证**：所有结果均为仿真实，缺乏流片后的真实硅片测试。模拟/混合信号设计中，仿真与实测结果往往存在较大差异（如工艺偏差、噪声、寄生效应等）。
*   **系统级验证缺失**：没有在真实AI任务上（如CIFAR-10）进行系统级仿真，因此无法评估该架构对神经网络最终准确率的影响。
*   **对比不严谨**：与不同工艺、不同特性的先前工作直接对比TOPS/W，虽然能指示趋势，但并非一个公平的标杆测试。

（完）
