---
title: Taking Cryptography Out of the Data Path via Near-Memory Processing in DRAM
title_zh: 基于DRAM近内存处理将密码学从数据路径中移除
authors: "Nicola Barcarolo, Brahmaiah Gandham, Mohammad Sadrosadati, Roberto Passerone, Onur Mutlu, Flavio Vella"
date: 2026-05-19
pdf: "https://arxiv.org/pdf/2605.20047v1"
tags: ["query:cim"]
score: 9.0
evidence: DRAM中近存计算加速密码学
tldr: 加密算法因处理器-内存数据搬运导致性能瓶颈。本文利用UPMEM PIM架构实现近内存计算，在DRAM内部直接执行AES-128和SHA-256。单rank性能低于CPU，但多rank并行时显著加速。展示了真实PIM系统在加密算法中的潜力与局限，为内存计算应用提供实证。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.20047v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 822, \"height\": 408, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.20047v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1197, \"height\": 376, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.20047v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1442, \"height\": 376, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.20047v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1441, \"height\": 452, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.20047v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1442, \"height\": 487, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.20047v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1442, \"height\": 495, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.20047v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1443, \"height\": 486, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.20047v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 925, \"height\": 453, \"label\": \"Table\"}]"
motivation: 传统处理器架构处理加密算法受限于内存带宽，增加延迟和能耗，PIM可减少数据移动，但缺乏真实系统验证。
method: 基于UPMEM PIM架构，在DRAM中实现AES-128和SHA-256，评估单rank及多rank下的性能与可扩展性。
result: 单rank性能低于现代CPU，但利用全部rank时，PIM能更有效地加速加密算法，吞吐量显著提升。
conclusion: 真实PIM系统通过多rank并行可突破内存瓶颈，有效加速加密算法，但需注意单rank性能局限。
---

## 摘要
AES-128和SHA-256等密码算法是确保数据安全性和完整性的基础。尽管这些算法计算效率高，但由于内存瓶颈，其性能常受限于以处理器为中心的架构（如CPU、GPU）。这一限制导致处理大量数据时延迟增加、能耗升高。为克服这些挑战，内存内处理（PIM）作为一种有前景的架构范式应运而生，允许计算直接在内存单元内部或附近进行。通过最小化处理器与内存单元之间的数据移动，PIM可显著加速密码算法并提升能效。先前多项工作已证实PIM在从根本上加速密码算法方面的有效性。然而，尚无工作广泛展示真实PIM系统的潜力。本文旨在探究真实PIM在加速密码算法方面的潜力与局限性。作为方法的一部分，我们采用UPMEM PIM架构评估密码算法的可扩展性。当这些算法在单个rank上运行时，其性能仍低于现代CPU。但将计算分布在多个rank上可显著提升性能。当利用所有可用rank时，真实PIM能更有效地加速密码算法。

## Abstract
Cryptographic algorithms such as AES-128 and SHA-256 are fundamental to ensuring data security and integrity. Although these algorithms are computationally efficient, their performance is often constrained by the processor-centric architectures (e.g., CPUs, GPUs), primarily due to the memory bottleneck. This constraint leads to increased latency and higher energy consumption, particularly when handling large volumes of data. To overcome these challenges, Processing-in-Memory (PIM) has emerged as a promising architectural paradigm, allowing computation to occur directly within or near memory units. By minimizing data movement between the processor and memory units, PIM can significantly accelerate cryptographic algorithms while improving energy efficiency. Several pieces of prior work have demonstrated the effectiveness of PIM at fundamentally accelerating cryptographic algorithms. However, none of the prior works have extensively demonstrated the potential of a real-world PIM system. In this paper, we want to investigate the potential and limitations of real-world PIM in accelerating cryptographic algorithms. As part of our methodology, the UPMEM PIM architecture is used to assess the scalability of cryptographic algorithms. When these algorithms operate on a single rank, their performance remains below that of modern CPUs. However, distributing the computation across multiple ranks significantly enhances performance. When all available ranks are utilized, real-world PIM can accelerate cryptographic algorithms more effectively.

---

## 论文详细总结（自动生成）

# 论文结构化总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：AES-128和SHA-256等密码算法在传统处理器中心架构（CPU/GPU）上运行时，性能受限于处理器与内存之间的数据移动瓶颈（内存瓶颈）。数据移动可占系统总能耗的62%，导致高延迟和高能耗，尤其是在处理大量数据时。
- **研究动机**：Processing-in-Memory（PIM）架构通过在内存内部或附近集成计算单元，减少数据移动，有望加速内存密集型应用。尽管先前有工作通过仿真或原型验证了PIM加速密码算法的潜力，但缺乏在真实商用PIM系统上的全面评估，尤其是SHA-256从未在真实PIM系统上实现过。
- **整体含义**：本文首次在真实PIM系统（UPMEM）上实现并评估AES-128和SHA-256，探究PIM在密码学加速中的潜力与局限，为内存计算范式提供实证基础。

## 2. 论文提出的方法论：核心思想、关键技术细节、算法流程

- **核心思想**：利用UPMEM PIM架构的DPU（DRAM Processing Unit）在内存中直接执行加密/哈希操作，避免数据通过窄带宽总线搬运，利用多DPU并行加速。
- **关键技术细节**：
  - 采用Processing-Near-Memory（PNM）方式，每个DPU是32位RISC核心，带24 KB指令SRAM、64 KB工作SRAM、64 MB主存储器MRAM。
  - 使用SPMD编程模型，所有DPU执行相同程序，操作不同数据。
  - 每个DPU可运行最多24个任务槽（tasklet），推荐至少11个以填满13级流水线。
  - AES-128：利用预计算查找表优化有限域乘法；密钥扩展在CPU上串行完成后广播给所有DPU；每个DPU加密不同的数据块。
  - SHA-256：由于单个消息的哈希依赖前序块，无法简单并行化；因此并行处理1024条不同消息，每条32 KB，总计32 MB数据。
- **算法流程**（以AES-128为例）：
  1. 主机CPU执行密钥扩展，广播给所有DPU。
  2. 将8 MB数据缓冲区划分为等大小块，线性分配给DPU，并行传输。
  3. 同时启动所有DPU执行加密，主机阻塞等待。
  4. 并行从DPU回传结果至主机内存。
- **多rank优化**：对多rank（一组64个DPU）采用异步传输（PIM #1）或异步执行（PIM #2），实现流水线重叠，减少空闲时间。

## 3. 实验设计：数据集/场景、Benchmark、对比方法

- **数据集/场景**：
  - 使用随机生成的字节数据。
  - AES-128：加密8 MB缓冲区。
  - SHA-256：对32 MB数据（1024条消息×32 KB）进行哈希。
- **Benchmark**：
  - 任务槽扩展（Tasklet scaling）：固定问题大小，变化单个DPU内任务槽数（1~24）。
  - 强扩展（Strong scaling）：变化单个rank内DPU数量（1~64），固定总数据量。
  - 弱扩展（Weak scaling）：保持每个DPU问题大小不变，变化DPU数量（单rank内、多rank至40个rank共2560 DPU）。
  - 多rank模型对比：异步传输（PIM #1） vs 异步执行（PIM #2）。
- **对比方法**：
  - 主机CPU上的软件实现（相同AES-128/SHA-256代码）。
  - 主机CPU使用AES-NI硬件指令集加速的AES-128。
  - 自身PIM实现的各版本对比（不同任务槽数、DPU数、rank数）。

## 4. 资源与算力

- **文中明确说明**：
  - 真实UPMEM机器配备20个PIM DIMM，每个DIMM含128个DPU（即2个rank），总DPU数2560（实际可用略少因坏块）。
  - DPU工作频率450 MHz。
  - 总PIM内存160 GB，内存带宽2.56 TB/s。
  - 主机CPU：Intel Xeon Silver 4215（非PIM部分使用4×64 GB DDR4-2666 DRAM）。
  - 使用UPMEM SDK 2023.2.0，LLVM 12编译器。
  - 未提及GPU或其他加速器，也未报告具体训练时长（因不涉及训练）。

## 5. 实验数量与充分性

- **实验组数**：
  - 任务槽扩展：AES-128和SHA-256各一组（图3a, 3b）。
  - 强扩展：每个算法在单rank内变化DPU数量（图4a, 4b）。
  - 弱扩展：单rank内和40个rank范围内（图5a, 5b, 6a, 6b）。
  - 多rank异步模型对比（PIM #1 vs PIM #2）：图6, 图7。
- **充分性评估**：
  - 实验覆盖了关键维度（并行度、数据规模、多rank优化），但缺少：
    - 不同密钥长度（如AES-256）的对比。
    - 不同输入数据大小（仅8 MB/32 MB）的敏感性分析。
    - 能耗测量（文中仅引用了历史数据，未实测）。
    - 与GP-GPU或FPGA的对比（仅与CPU对比）。
  - 实验整体充分，但范围有限，结论更适用于UPMEM这一特定PIM系统。

## 6. 论文的主要结论与发现

- **关键发现**：
  1. 任务槽数≤11时性能近线性提升，超过11后流水线饱和，性能不变。
  2. 单rank内DPU数量扩展时，DPU内核时间近线性减小，但CPU-DPU传输时间线性增长，可成为瓶颈。
  3. 弱扩展下执行时间恒定，传输时间线性增加；异步模型（PIM #1和PIM #2）可缓解传输瓶颈，但DPU间无法直接通信。
  4. 单rank配置下PIM性能低于现代CPU软件实现（甚至远低于AES-NI硬件加速）。
  5. 利用全部40个rank时，PIM可显著加速，PIM #1（异步传输）性能优于PIM #2（异步执行）。
  6. 从绝对吞吐量看，全PIM配置下AES-128和SHA-256的性能超越了CPU纯软件实现，但在AES-128上仍落后于AES-NI（图6a）。
- **主要结论**：真实PIM系统通过多rank大规模并行可有效加速内存密集型密码算法，但受限于单rank性能、传输带宽和缺乏高级算术支持。

## 7. 优点：方法或实验设计亮点

- **首次实现SHA-256于真实PIM系统**：填补了该领域空白。
- **完整的多维度可扩展性分析**：从任务槽、DPU、rank到异步模式，逻辑清晰。
- **对比公平**：PIM实现与主机CPU上相同代码以及AES-NI硬件加速均对比，明确显示PIM的优势与局限。
- **公开可复现的基准**：基于UPMEM商用平台，实验环境和代码可被后续研究重复。
- **实用优化**：利用预计算查找表、本地缓存、异步传输等技巧，展示了真实PIM编程经验。

## 8. 不足与局限

- **实验覆盖有限**：
  - 仅测试了AES-128和SHA-256，未覆盖AES-256、其他哈希函数（如SHA-3）或分组密码模式（如GCM）。
  - 仅使用固定数据规模（8 MB/32 MB），未测试不同粒度的影响。
  - 未测量能耗，仅引用外部数据，缺乏实际功耗对比。
- **硬件限制**：
  - DPU不支持浮点、乘除等复杂运算，通过软件模拟，性能受限。
  - DPU间无直接通信，数据交换仍需通过窄带总线，限制某些算法（如带反馈模式的加密）。
  - 异步传输仅支持跨rank，不支持同rank内DPU，降低了并行灵活性。
- **对比偏差风险**：
  - 与非PIM CPU对比时，CPU实现是相同串行代码，但未使用多核CPU并行优化（如OpenMP），可能低估CPU性能潜力。
  - AES-NI硬件实现远优于PIM，论文未深入探讨PIM与专用加速器的竞争地位。
- **应用限制**：PIM加速仅适用于可高度并行化、无数据依赖的内存密集型阶段；密钥扩展等串行部分仍需CPU处理，且传输时间在数据量极大时可能抵消增益。

（完）
