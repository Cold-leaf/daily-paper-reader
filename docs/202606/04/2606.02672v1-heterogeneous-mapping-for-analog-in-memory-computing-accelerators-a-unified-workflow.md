---
title: "Heterogeneous Mapping for Analog In-Memory Computing Accelerators: A Unified Workflow"
title_zh: 面向模拟存内计算加速器的异构映射：一种统一工作流
authors: Corey Lammie
date: 2026-06-01
pdf: "https://arxiv.org/pdf/2606.02672v1"
tags: ["query:cim"]
score: 9.0
evidence: 模拟存内计算（AIMC）加速器，用于DNN推理
tldr: 模拟内存计算加速器通过异构架构结合模拟计算单元和数字处理器，以应对有限有效精度问题。现有方法按映射粒度、优化策略和模型支持分类，提炼为统一四阶段工作流。将该工作流的前两个阶段应用于GPT-2，首次生成解码器专用transformer的精度敏感度谱，发现敏感度集中在49个投影中的4个，第一个解码器块的注意力输出主导。结果表明投影级映射和选择性数字执行对可靠部署至关重要。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.02672v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 891, \"height\": 935, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.02672v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 908, \"height\": 194, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.02672v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1806, \"height\": 517, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.02672v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1881, \"height\": 387, \"label\": \"Table\"}]"
motivation: 模拟内存计算加速器有效精度有限，亟需异构架构及将DNN工作负载在模拟和数字资源间映射的系统方法。
method: 分类现有映射方法并提炼为统一四阶段工作流，并将其前两个阶段应用于GPT-2模型生成精度敏感度谱。
result: GPT-2的精度敏感度集中于49个投影中的4个，首个解码器块的注意力输出敏感度高出其他投影一个数量级。
conclusion: 投影级映射和选择性数字执行早期块及输出投影是可靠部署的基础，为异构AIMC加速器设计提供指导。
---

## 摘要
模拟存内计算加速器直接在存储阵列中执行矩阵-向量乘法，减少数据移动并提高DNN推理效率。其有限的有效精度促使了将模拟计算片与数字处理单元相结合的异构架构。本文根据映射粒度、优化策略和模型支持，对现有的在这些资源间划分DNN工作负载的方法进行了分类，并将其提炼为统一的四阶段工作流。为了在现有方法尚未涉及的模型类别上展示该工作流，我们将前两个阶段应用于GPT-2，生成了首个针对解码器-only Transformer的AIMC特定精度敏感性剖面。敏感性主要由49个投影中的4个主导，其中第一个解码器块的注意力输出主导了一个数量级。这表明投影级映射以及对早期块和面向输出的投影的选择性数字执行对于在AIMC硬件上可靠部署解码器Transformer至关重要。

## Abstract
Analog In-Memory Computing (AIMC) accelerators execute matrix-vector multiplications directly within memory arrays, reducing data movement and improving DNN inference efficiency. Their limited effective precision motivates heterogeneous architectures that combine analog compute tiles with digital processing units. This letter classifies existing methods for partitioning DNN workloads across these resources by mapping granularity, optimization strategy, and model support, and distills them into a unified four-stage workflow. To demonstrate the workflow on a model class not yet addressed by existing methods, we apply its first two stages to GPT-2, producing the first AIMC-specific precision sensitivity profile for a decoder-only transformer. Sensitivity is dominated by 4 of 49 projections, with the first decoder block's attention output dominating by an order of magnitude. This suggests that projection-level mapping and selective digital execution of early-block and output-facing projections are important for reliable decoder-transformer deployment on AIMC hardware.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：模拟存内计算（AIMC）加速器通过在存储阵列内直接执行矩阵-向量乘法，大幅减少数据移动、提升DNN推理能效。但其有限的有效精度（通常3~8比特）限制了在精度敏感操作上的应用。
- **核心问题**：如何将DNN工作负载**异构映射**到AIMC加速器中的模拟计算片（tiles）和数字处理单元（CUs），以在精度、能耗和延迟之间取得联合最优。现有映射方法碎片化、覆盖率不足（仅支持CNN、部分支持编码器Transformer，但未涉及解码器Only的LLM）。
- **整体含义**：本文通过分类现有方法、提炼统一工作流，并首次在GPT-2解码器Transformer上生成AIMC特定精度敏感性剖面，为异构映射研究提供了标准化框架和新的基准。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将异构映射问题分解为四个可迭代的阶段，并从现有九种方法中抽象出统一流程。
- **关键技术细节**：
  - **阶段1：架构表征**。配置片数、数字CU能力（INT8/FP16/FP32）、I/O精度、存储层次、互连拓扑；评估候选分片策略在阵列利用率、权重复用、算术强度上的影响；建立代理模型（如模拟MAC比、数字权重大小）；对非易失存储需建模时序精度退化。
  - **阶段2：精度敏感性分析**。采用**逐投影扰动分析**（每个投影单独切换为模拟运行，其余保持FP32）计算PPL提升Δℓ；替代方法包括Hessian特征分析、激活显著度。该分析可剪枝搜索空间、确定适应顺序。发现早期层和输出层通常更敏感。
  - **阶段3：架构感知映射**。应用启发式、解析式或基于学习（如超网络）的优化策略，在阶段2引导下分配操作到模拟/数字单元；可联合调整层维度以改善片利用率；输出帕累托映射集（精度-能耗/延迟权衡）。
  - **阶段4：系统级评估**。使用架构模拟器（DNN+NeuroSim、ALPINE、3DCiM等）或物理硬件验证所选映射；建议报告任务精度（含误差棒）、模拟MAC比、端到端延迟、每次推理能耗、映射搜索计算成本。

### 3. 实验设计：使用了哪些数据集/场景，benchmark，对比了哪些方法

- **数据集**：WikiText-103（用于GPT-2-small的困惑度评估）。
- **场景**：解码器Only Transformer（GPT-2-small，124M参数，12个解码器块，49个权重投影）。
- **Benchmark**：自身生成的精度敏感性剖面（无其他对比方法，因为这是首次针对此类模型）。论文在讨论中提及现有方法（PAWDD、CIMQ、Harmonica、LionHeart、MPS等）但未进行直接对比实验，而是将其作为分类基础。
- **对比方法**：未进行算法间对比；实验仅用于展示统一工作流的前两个阶段，并验证敏感性剖面特性。

### 4. 资源与算力：使用了多少算力（GPU型号、数量、训练时长等）

- **文中明确说明**：未提及使用的具体GPU型号、数量或训练时长。仅提到使用开源框架AIHWKIT进行模拟转换。整个扰动研究需要490次前向传播（49个投影×10次噪声实现），计算开销较低。
- **推断**：GPT-2-small的模型较小，490次前向传播在单GPU上可在较短时间内完成，但具体资源未知。论文未标注任何训练/推理的硬件配置。

### 5. 实验数量与充分性：大概做了多少组实验，是否充分、客观、公平

- **实验数量**：
  - 主实验：对GPT-2-small的49个投影逐一进行扰动测试，每个投影独立运行10次不同噪声种子，共490次前向传播。
  - 无其他消融实验、无不同噪声模型对比、无联合扰动实验。
- **充分性评价**：
  - **优点**：覆盖了所有投影类型，统计了均值和标准差，揭示了关键模式（如首个注意力输出敏感度高出其他投影一个数量级）。
  - **不足**：
    - 仅针对单一模型（GPT-2-small），未验证更大模型或不同架构族。
    - 仅在单一噪声模型（PCM校准的4比特有效精度）下进行，未做跨SRAM/ReRAM噪声模型验证。
    - 仅考察边际敏感性，未检测多投影同时模拟时的交互效应（作者承认这点，但指出这是所有阶段3方法的输入标准）。
    - 未与现有映射方法的实际端到端结果对比（因为阶段3-4未完整执行）。
- **客观公平性**：扰动方法本身是标准且可复现的，但缺乏与其他基线方法在相同设置下的比较，因此无法评估该方法在整体映射性能上的优劣。

### 6. 论文的主要结论与发现

- **核心发现**：GPT-2-small的精度敏感性高度集中在少数投影上——49个投影中仅4个Δ perplexity>10，其中**首个解码器块的注意力输出投影（c_proj）**以Δ=33.1领先一个数量级。第二梯队包括块1-2的FFN升投影和块0的FFN降投影。
- **模态依赖**：
  - 早期层和输出层（LM Head及最后块注意力输出）表现出敏感，但内部投影（尤其是中间块的注意力输出）具有极高噪声容忍度（Δ<0.22）。
  - 同一块内不同投影类型（Q/K/V vs 注意力输出 vs FFN）灵敏度差异显著，表明**块级粒度是次优的**，必须采用投影级映射。
- **实用建议**：约82%的投影（40/49）Δ<3，可保守映射到模拟片；早期块及面向输出的投影需保留在数字CU。该剖面可直接初始化超网络路径权重或确定重训练顺序。

### 7. 优点：方法或实验设计上的亮点

- **统一工作流的提出**：整合了现有碎片化方法，提供标准化四阶段流程，便于后续研究者系统性地解决异构映射问题。
- **首次覆盖解码器Transformer**：填补了现有工作对LLM架构（GPT、LLaMA等）的空白，生成的敏感性剖面是后续阶段3方法（如LionHeart、MPS）的可重用输入。
- **扰动方法的高效性**：仅需O(L)前向传播（L为层数），可轻松扩展到数十亿参数模型（线性增长）。
- **粒度分析深刻**：揭示了投影级粒度的重要性，挑战了传统块级或层级映射的默认假设，对实际硬件设计有直接指导意义。
- **开放讨论**：指出了可扩展性、时序退化、社区基准等关键开放挑战，具备前瞻性。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制

- **实验覆盖有限**：
  - 仅验证了GPT-2-small（124M），未测试更大模型（如GPT-3、LLaMA-7B以上），也未测试其他架构族（如Mamba、RWKV等SSM）。
  - 仅使用单一噪声模型（PCM），未对比SRAM、ReRAM等不同模拟存储技术的噪声特征，也未建模时间漂移对敏感度排序的潜在影响。
- **评估不完整**：未执行工作流中的阶段3和阶段4，即未实际产生一个异构图映射并评估端到端能耗/延迟，缺少与现有映射方法（如LionHeart、MPS）在同一设置下的公平比较。
- **方法偏差风险**：逐投影扰动仅捕捉边际效应，忽略联合噪声放大效应——当多个投影同时模拟时，实际退化可能高于边际和，导致敏感度排名发生变化。作者承认此点，但认为联合效应将在阶段3中得到补偿（如重训练）。
- **应用限制**：
  - 短期内无法直接应用于部署，因为缺乏可泛化的硬件配置映射结果。
  - 对KV缓存动态存储、预填充/解码不对称资源需求等LLM特有的问题仅提及未建模。
  - 社区缺乏标准化基准（参考架构、精度模型、评估指标），导致不同方法结果难以直接比较。

（完）
