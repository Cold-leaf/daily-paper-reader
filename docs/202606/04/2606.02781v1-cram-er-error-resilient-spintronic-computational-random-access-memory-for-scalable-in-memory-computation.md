---
title: "CRAM-ER: Error-Resilient Spintronic Computational Random Access Memory for Scalable In-Memory Computation"
title_zh: CRAM-ER：面向可扩展存内计算的容错自旋计算随机存取存储器
authors: "Sohan Salahuddin Mugdho, Md. Shahedul Hasan, Brahmdutta Dixit, Yang Lv, Jian-Ping Wang, Cheng Wang"
date: 2026-06-01
pdf: "https://arxiv.org/pdf/2606.02781v1"
tags: ["query:cim"]
score: 9.0
evidence: 直接面向存内计算架构，提出容错CRAM设计
tldr: 深度神经网络虽性能卓越，但传统冯·诺依曼架构面临内存瓶颈。基于MRAM的CRAM存内计算虽能消除外围开销，却受概率性开关误差限制，且大量串行写入导致吞吐低下。为此，提出CRAM-ER架构，通过混合自旋-CRAM与CMOS加法器树，并结合错误感知模型微调与细粒度纠错，实现可扩展的高可靠矩阵向量乘法。在DNN任务上，CRAM-ER将延迟降低两个数量级，精度近无损，能效与能量延迟积均优于CPU/GPU+高带宽DRAM，为大规模存内计算提供了高效可靠方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.02781v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 832, \"height\": 674, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.02781v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1783, \"height\": 673, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.02781v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1809, \"height\": 526, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.02781v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 882, \"height\": 476, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.02781v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 838, \"height\": 1664, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.02781v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 857, \"height\": 462, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.02781v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 795, \"height\": 429, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.02781v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 795, \"height\": 551, \"label\": \"Table\"}]"
motivation: 解决CRAM因概率性MRAM开关产生的门级错误和低吞吐量问题，使其能可靠加速DNN。
method: 提出CRAM-ER架构，采用混合自旋-CRAM与CMOS加法器树，结合错误感知模型微调和细粒度纠错。
result: 在DNN基准测试中，接近无损精度，CRAM延迟降低达2数量级，能效和能量延迟积优于CPU/GPU+高带宽DRAM。
conclusion: CRAM-ER实现了高能效、高可靠和高性能的存内计算，为大规模DNN加速提供可行方案。
---

## 摘要
深度神经网络（DNN）已在多个领域取得最先进性能。然而，传统的冯·诺依曼计算范式面临严重的存储瓶颈。新兴的近存储和存内计算方法缓解了这一问题，但引入了显著的外围开销。基于MRAM的计算随机存取存储器（CRAM）能够实现原位逻辑计算而无外围开销，提供了一种高密度、高能效的解决方案。然而，概率性MRAM开关引入了门级错误，限制了CRAM加速DNN的可扩展性和可靠性。此外，大量顺序MRAM写入严重制约了CRAM的吞吐量。为应对这些挑战，我们提出了一种容错CRAM（CRAM-ER）架构，用于可扩展的存内矩阵向量乘法（MVM）。我们的错误感知硬件-软件协同设计框架采用混合自旋CRAM+CMOS加法树架构，以减轻器件级错误的影响，实现了高面积和能效的MVM功能。我们进一步开发了错误感知模型微调和细粒度错误校正以增强容错性。在DNN基准测试上对CMOS+自旋混合架构的评估显示，在将CRAM延迟降低多达两个数量级的同时实现了近乎无损的精度，在能效和能量延迟积方面均优于CPU/GPU+高带宽DRAM。

## Abstract
Deep neural networks (DNNs) have achieved state-of-the-art performance across diverse domains. However, typical Von Neumann compute paradigms face severe memory bottlenecks. Emerging near-memory and compute-in-memory approaches alleviate this but incur significant peripheral overhead. Computational Random Access Memory (CRAM) based on MRAM enables in-situ logic without peripheral overhead, offering a dense, energy-efficient solution. However, probabilistic MRAM switching induces gate-level errors that limit the scalability and reliability of CRAM for accelerating DNN. Moreover, the large number of sequential MRAM writes severely constrains CRAM throughput. To address these challenges, we propose an error-resilient CRAM (CRAM-ER) architecture for scalable in-memory matrix-vector multiplications (MVMs). Our error-aware hardware-software co-design framework leverages a hybrid spintronic-CRAM + CMOS adder-tree architecture to mitigate the impact of device-level errors, demonstrating MVM functionality with high area and energy efficiency. We further develop an error-aware model fine-tuning and fine-grained error correction for enhanced error resilience. Evaluations of the CMOS+spintronic hybrid architecture on DNN benchmarks show near-lossless accuracy while reducing CRAM latency by up to 2 orders of magnitude, outperforming CPU/GPU+high-bandwidth DRAM in both energy efficiency and energy-delay product.

---

## 论文详细总结（自动生成）

### 论文中文总结

#### 1. 核心问题与整体含义（研究动机与背景）
*   **背景**：深度神经网络（DNN）性能优异，但传统冯·诺依曼架构中数据频繁搬移造成严重**内存墙**问题。
*   **现有方案局限**：近存计算和存内计算（CiM）能缓解瓶颈，但模拟CiM受限于ADC等外围开销，数字CiM面积大；基于MRAM的计算随机存取存储器（CRAM）可实现原位逻辑且外围开销低，具有高密度、高能效潜力。
*   **核心挑战**：
    *   **概率性开关误差**：STT-MRAM的写入是概率性的，导致CRAM中基本门（如NAND）产生非零错误率，且在多比特MAC操作中逐级累积，严重损害DNN精度。
    *   **写入吞吐瓶颈**：CRAM中大量顺序的MRAM写入操作（尤其是加法树中的累加）导致延迟极高，限制了系统吞吐量。
*   **目标**：提出一种可扩展、高可靠的CRAM架构（CRAM-ER），实现多比特DNN的存内加速，在保持能量效率的同时克服上述挑战。

#### 2. 方法论：核心思想与技术细节
*   **核心思想**：**硬件-软件协同设计**，通过**混合CMOS+自旋CRAM架构** 划分任务，结合**细粒度错误校正**与**错误感知模型微调**，在控制面积开销的前提下同时降低错误率和写入延迟。
*   **关键技术细节**：
    1.  **混合CRAM+CMOS加法树架构**：
        *   将MAC操作拆分：所有**元素乘法**和**大部分累加**在CRAM内完成（利用CRAM的高密度、高能效并行性）；剩余部分（如关键进位路径）由**CMOS数字加法树**完成，从而避免全CRAM累加带来的错误累积和高写入延迟。
        *   通过调整**加法树比例**（例如25%累加由CMOS执行）实现精度-面积-延迟的最优折中。
    2.  **选择性位级错误校正（EC）**：
        *   基于MAJ3电路：对每个输出的最高有效进位位进行三次重复计算并进行多数表决，仅对最关键的位进行纠错，降低能耗和延迟开销。
    3.  **错误感知模型微调（Fine-tuning）**：
        *   建立从器件级（STT概率模型）到门级（NAND错误率）的仿真框架，统计给定阵列下MVM输出的**位级错误率**。
        *   在PyTorch中，将估计的位级错误率嵌入DNN前向传播过程，对量化模型进行微调，使权重适应固定的计算错误模式，实现近乎无损的精度。
*   **公式与算法流程（文字说明）**：
    *   概率写入模型：`P_SW = 1 - exp(-tp/τ)`，其中τ与热稳定因子Δ、电压相关。
    *   NAND门错误率依赖于TMR，TMR越大误差越小（使用133%作为保守值）。
    *   MAC构建：全CRAM需先构建全NAND全加器→脉动乘法器→树加法器，操作数极大；CRAM-ER将树加法器部分转移至CMOS。

#### 3. 实验设计
*   **数据集**：MNIST（简单）、CIFAR-10（中等复杂度）。
*   **模型基准**：
    *   LeNet-5 (MNIST)
    *   ResNet-20 (CIFAR-10)
    *   ResNet-18, ViT-b/4 (CIFAR-10，用于系统级评估)
*   **对比方法**：
    *   **精度对比**：Q-baseline（4b量化基线）、CRAM only（无任何纠错/混合）、CRAM+EC（仅加纠错）、CRAM+EC+Finetune（加微调）。同时比较不同**加法树比例**（0%, 12.5%, 25%, 50%, 100%）。
    *   **系统级对比**：近存计算（NMC）平台：HMC + Xeon CPU、HBM2 + A100 GPU；标准CRAM；CRAM-ER不同配置；以及基于先进器件（超快写入）的投影配置CRAM-ER-P。
*   **评估指标**：推理准确率、总能量（J）、总延迟（s）、能量延迟积（EDP）、能量延迟面积积（EDAP）。

#### 4. 资源与算力
*   **文中未明确说明**训练/微调所用的GPU型号、数量及时长。
*   系统级对比中引用了Intel Xeon Platinum 8480 CPU和Nvidia A100 GPU的性能数据作为参考，但未说明CRAM-ER本身的训练硬件。
*   （推测：微调在常见GPU工作站上完成，但论文未提及，属于信息缺失。）

#### 5. 实验数量与充分性
*   **实验组数丰富**：
    *   **精度实验**：2个数据集 × 2个模型（LeNet-5, ResNet-20）× 5种加法树比例 × 4种配置（CRAM only, +EC, +EC+Finetune）= 约40组（部分未全部列出，但表2有详细条目）。
    *   **系统级实验**：4个DNN模型（LeNet-5, ResNet-20, ResNet-18, ViT-b/4）× 多种平台对比（5-6种） + 投影版本，呈现了能量、延迟、EDP、EDAP多条曲线。
    *   **消融实验**：对比了有无EC、有无微调、不同加法树比例，充分验证各组件贡献。
*   **客观性**：使用标准基准和开源模型（LeNet, ResNet, ViT），对比方法包括当前主流CPU/GPU+高带宽内存，设计数量级衡量公平。
*   **充分性评价**：实验覆盖了从简单到复杂的DNN，从算法精度到系统级能效，从现成器件到投影器件，较为全面。不足：缺乏实际芯片流片验证，全仿真依赖模型假设；仅测试了CIFAR-10和MNIST，未在ImageNet等更大规模数据集上验证。

#### 6. 主要结论与发现
*   **精度**：通过CRAM-ER（25%加法树+EC+微调），在MNIST上准确率达98.64%（基线98.65%），在CIFAR-10上达89.17%（基线89.77%），接近无损。
*   **延迟**：相比标准CRAM，CRAM-ER将延迟降低**20~200倍**，消除了大量MRAM顺序写入瓶颈。
*   **能效**：相比HBM2+A100 GPU，CRAM-ER实现**10倍**能量节省（投影版可达70倍）。
*   **能量延迟积（EDP）**：CRAM-ER(25%)相比GPU提升**2倍**，投影版提升**16倍**。
*   **面积折中**：25%加法树配置在EDAP指标上取得最优，进一步增加加法树虽能提升精度但造成面积浪费。

#### 7. 优点
*   **硬件-软件协同设计**：将器件级概率错误、电路级纠错、算法级微调有机整合，实现端到端优化。
*   **精确识别关键瓶颈**：明确指出了概率错误累积和大量MRAM写入是CRAM难以扩展的主因。
*   **高效混合架构**：只在关键进位路径使用CMOS加法树，既保留CRAM的高密度/高能效并行乘法，又用CMOS消除错误和延迟瓶颈，面积开销低。
*   **细粒度纠错策略**：只对最高有效位进行纠错，避免全面纠错的高开销。
*   **前瞻性评估**：考虑了未来器件（超快SOT-MRAM），展示了性能进一步提升的潜力。

#### 8. 不足与局限
*   **缺乏芯片实测**：所有结果基于仿真（Verilog + NVSim + 自建Python框架），缺少流片验证，实际器件变异、温度效应等未完全建模。
*   **数据集规模有限**：仅使用MNIST和CIFAR-10（32×32图像），未在ImageNet等高分辨率、大类别数据集上评估，对复杂视觉任务的可扩展性存疑。
*   **误差模型简化**：全NAND加法器构建的CRAM操作级错误率基于固定TMR=133%，未考虑工艺波动或不同TMR的影响（注：文中指出更高TMR可进一步降低错误率，但未做敏感度分析）。
*   **精度损失边界**：在CIFAR-10上CRAM-ER(25%)仍有约0.6%的精度下降，能否通过更复杂的噪声注入微调弥补仍待研究。
*   **微调成本**：错误感知微调需要额外计算资源和数据，论文未报告训练代价（时间、能耗），可能影响实际部署的可重复性。
*   **对比对象选择**：与GPU的对比基于理想化假设（MAC能量按比例缩放至4b操作），且未考虑GPU自身的缓存优化等方法，可能略微高估CRAM优势。

（完）
