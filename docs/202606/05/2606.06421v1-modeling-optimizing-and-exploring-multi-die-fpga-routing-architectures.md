---
title: "Modeling, Optimizing and Exploring Multi-Die FPGA Routing Architectures"
title_zh: 多裸片FPGA路由架构的建模、优化与探索
authors: "Amirhossein Poolad, Soheil Gholami Shahrouz, Andrew Boutros, Vaughn Betz"
date: 2026-06-04
pdf: "https://arxiv.org/pdf/2606.06421v1"
tags: ["query:cim"]
score: 8.0
evidence: 面向3D集成电路的多裸片FPGA路由架构建模与优化
tldr: "多die FPGA（2.5D/3D）的互连路由架构设计因缺乏准确建模工具而难以优化。本文增强开源VTR工具，灵活建模多种多die路由架构，并改进VPR布局布线引擎，结合HSPICE对7nm工艺和45nm中介层进行电路建模。实验表明，3D FPGA相比2D可减少14%线长、改善6%关键路径延迟，2.5D FPGA在32%互连密度下仅增加2%线长和4%延迟。所有扩展已开源并集成至VTR主线，为多die FPGA路由架构设计提供了高效探索平台。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.06421v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 888, \"height\": 260, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.06421v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 909, \"height\": 654, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.06421v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 898, \"height\": 811, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.06421v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 917, \"height\": 447, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.06421v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 906, \"height\": 583, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.06421v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 932, \"height\": 311, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.06421v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 827, \"height\": 603, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.06421v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 927, \"height\": 562, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.06421v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 909, \"height\": 641, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.06421v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 914, \"height\": 714, \"label\": \"Table\"}]"
motivation: 缺乏准确通用的多die FPGA路由架构建模工具，导致2.5D和3D FPGA的互连设计问题未得到充分研究。
method: 增强VTR CAD工具灵活建模多die路由架构，改进VPR布局布线引擎，并基于HSPICE对7nm工艺和45nm中介层进行跨die电路建模。
result: "3D FPGA实现14%线长减少和6% CPD改善；2.5D FPGA在32%互连密度下仅带来2%线长和4% CPD开销。"
conclusion: 合适的互连路由架构使多die FPGA在提升容量的同时不显著牺牲可路由性和延迟，所有扩展已开源集成至VTR主线。
---

## 摘要
裸片堆叠技术通过将多个有源裸片集成在无源硅中介层上，实现了2.5D FPGA，从而提高了良率和容量，并为将多个有源裸片直接堆叠在一起的3D架构铺平了道路。在这些多裸片器件中，底层裸片堆叠技术的独特电气和物理特性对裸片间连接密度和延迟施加了限制，因此需要定制化的裸片间路由架构。然而，由于缺乏精确且多功能的建模工具，有关如何最佳设计裸片间路由架构的许多问题仍未得到解答。为弥补这一不足，我们增强了开源FPGA CAD工具VTR，使其能够灵活建模各种多裸片路由架构，并改进了VPR的布局和布线引擎，以优化2.5D和3D FPGA。我们采用7纳米工艺节点和45纳米硅中介层，在多种跨裸片技术下，对有源裸片的裸片间连接进行了基于HSPICE的电路建模。利用这一增强框架，我们对2.5D和3D FPGA中的裸片间路由架构进行了详细的设计空间探索，刻画了跨裸片技术、裸片间连接数量、扇入/扇出以及中介层导线长度对关键路径延迟(CPD)、线长、面积和可路由性的影响。结果表明，通过合适的裸片间路由架构，2.5D和3D FPGA可以在不显著牺牲可路由性或延迟的情况下增加容量。具体而言，与2D器件相比，3D FPGA实现了高达14%的线长减少和6%的CPD改善，即便在现有10微米间距技术下仍保持可路由性；而2.5D FPGA在32%的裸片间连接度下仅产生2%的线长和4%的CPD开销。所有扩展均已开源并与VTR主分支集成。

## Abstract
Die stacking has enabled 2.5D FPGAs by integrating multiple active dice on a passive silicon interposer for improved yield and capacity, and paved the way for 3D architectures that stack active dice directly atop one another. In these multi-die devices, the unique electrical and physical characteristics of the underlying die-stacking technology impose limitations on inter-die connection density and latency, necessitating a bespoke inter-die routing architecture. However, the absence of accurate and versatile modeling tools has left most questions about how to best design the inter-die routing architecture unanswered.   To address this gap, we enhance the open-source FPGA CAD tool VTR to flexibly model a wide range of multi-die routing architectures, and augment VPR's placement and routing engines to improve optimization for both 2.5D and 3D FPGAs. We perform HSPICE-based circuit modeling of inter-die connections for active dice using a 7 nm process node and a 45 nm silicon interposer across several die-crossing technologies. Using this enhanced framework, we conduct a detailed design space exploration of inter-die routing architecture in 2.5D and 3D FPGAs, characterizing the impact of die-crossing technology, inter-die connection count, fan-in/fan-out, and interposer wire length on critical path delay (CPD), wirelength, area, and routability.   Our results show that with suitable inter-die routing architectures, 2.5D and 3D FPGAs can increase capacity without significant routability or delay penalties. Specifically, 3D FPGAs achieve up to 14% wirelength reduction and 6% CPD improvement over 2D devices, and remain routable even with existing $10\,μ$m pitch technologies, while 2.5D FPGAs incur only a 2% wirelength and 4% CPD overhead at 32% inter-die connectivity. All extensions are open source and integrated with the VTR master branch.

---

## 论文详细总结（自动生成）

# 多裸片FPGA路由架构的建模、优化与探索——论文总结

## 1. 核心问题与整体含义（研究动机和背景）

随着深度学习加速、ASIC仿真、数据中心网络卸载等应用对FPGA逻辑容量需求不断增长，传统方案（大尺寸单裸片或离散多FPGA集群）面临良率瓶颈和片间高延迟、低带宽问题。先进封装技术（2.5D硅中介层集成、3D裸片堆叠）通过集成多个较小裸片来提升容量，但裸片间连接受物理技术限制（如凸点间距、导线长度），导致连接密度和延迟出现新约束。现有研究缺乏能够同时准确描述2.5D和3D FPGA裸片间路由架构参数（连接密度、扇入/扇出、中介层导线长度等）并支持端到端评估的通用建模工具，使得如何设计最优裸片间路由架构成为未解难题。

## 2. 方法论

### 核心思想
- **物理层面**：基于HSPICE对2.5D/3D裸片间连接进行详细的电路建模，考虑驱动级、多路选择器、ESD保护、中介层/堆叠互连的RC寄生，得到准确的延迟和面积数据。
- **架构描述层**：在开源VTR框架中引入**Scatter-Gather (SG) 语法**，灵活定义裸片间连接的模式（扇入Fg、扇出Fsc、导线长度、方向性、空间偏移），并支持2.5D的中断边界（`<interposer_cut>`）和3D的垂直CHANZ节点。
- **CAD优化层**：
  - 路由资源图构建：对3D引入CHANZ节点，对2.5D在导线生成时自动拆分跨越边界的线网。
  - 路由器启发式：采用**解耦的两分量启发式**（翻译不变的内裸片查找表 + 裸片穿越代价表），解决2.5D设备中查找表平移不变性被破坏的问题，路由时间降低2.5×。
  - 布局代价函数：引入**拥塞感知代价**（基于WLPA模型）专门针对2.5D裸片间通道，避免局部拥塞热点。

### 关键技术细节
- 中介层可行性分析公式：  
  $\alpha = L_{\text{int}} \cdot H_{LB}^2 / (W \cdot P^2)$  
  其中 $\alpha$ 为裸片间连接比率，$L_{\text{int}}$ 为中介层导线长度，$H_{LB}$ 为逻辑块高度，$W$ 为通道宽度，$P$ 为凸点间距。
- 电路模型：对于2.5D链路，信号路径包括源裸片硅层→通孔堆栈→顶层金属→第一个凸点→中介层导线→第二个凸点→目标裸片顶层金属→通孔堆栈。仿真中通过面积×延迟²最小化选择最佳驱动级尺寸。

### 算法流程（文字描述）
1. 根据设计规模自动确定最小的方形FPGA网格（最大化对互连资源的压力）。
2. 对2.5D设备，根据 `α` 和 `Lint` 决定是否足以布线（可行性检查）。
3. 使用SG语法定义裸片间路由模式，并附加到路由资源图中（3D：CHANZ节点；2.5D：在中断边界两侧嵌入裸片间节点）。
4. 布局阶段：基于最小化HPWL的模拟退火 + 对裸片间通道的拥塞代价项，鼓励信号在裸片内完成。
5. 路由阶段：A*时序驱动路由，使用解耦的查找表（内裸片部分从零裸片穿越代价采样，裸片穿越部分单独存储最小延迟）。
6. 对每个架构参数组合，运行全CAD流程（布局→布线→时序分析），收集CPD、WL、路由时间、面积等指标。

## 3. 实验设计

- **数据集/Benchmark**：使用 **Koios benchmark suite**（深度学习和高性能计算应用），电路规模从12k到759k原始元件，对应的FPGA网格尺寸从78×78到335×335个瓦片。
- **场景**：
  - **3D同质集成**：两个同质裸片垂直堆叠。评估技术：1μm混合键合（Hybrid Bond）、5μm混合键合、10μm微凸点（μbump）。参数扫描：`Nsg`（每逻辑块垂直连接数）、`Fg`/`Fsc`（扇入/扇出）取值 {64,32,16,8,4}。
  - **2.5D同质集成**：两个裸片并排放置在硅中介层上。评估技术：10μm微凸点、5μm混合键合。参数扫描：中介层导线长度 `Lint`（5~37个逻辑块行）、扇入 `Fg`（3~48）、连接密度（通过调整Nsg和Lint组合）。
- **对比方法**：2D单裸片基线（相同7nm工艺，相同逻辑结构）。
- **评价指标**：关键路径延迟（CPD，归一化）、布线总长度（WL）、路由时间、瓦片面积（含ESD面积节省）、面积-延迟积。

## 4. 资源与算力

论文**未明确说明**所使用的计算资源（如GPU型号、数量、训练时长），也未提及具体的服务器配置或总CPU路由时间（仅报告归一化路由时间）。因此无法提供精确算力信息。

## 5. 实验数量与充分性

- **实验数量**：
  - 3D实验：针对3种间距（1μm/5μm/10μm），每种间距下扫描5种Fg/Fsc组合（共15组），其中10μm还额外测试了Nsg=1的5组；另测试了25μm间距（部分失败）。总共约25+组架构。
  - 2.5D实验：表III中列出了三部分实验（共约30组不同参数组合），涵盖Lint扫描（8个值）、Fg扫描（5个值）、Nsg/密度扫描（7个值）。
  - 每个架构点采用多个Koios电路（未明确个数，但据“geometric mean of benchmark suite”推断覆盖了Koios中可用电路子集）。
- **充分性评估**：论文进行了**系统的设计空间探索**，覆盖了裸片间连接密度、扇入/扇出、中介层导线长度等关键参数，并对比了2D基线；针对3D还测试了不同间距下的可路由性边界（25μm失败）。实验设计合理，结果客观，消融分析清晰（如Lint vs CPD/WL/tradeoff）。**不足之处**在于：仅评估了同质集成（两个同构裸片），未涉及异构3D（如逻辑+存储器）；仅采用7nm工艺节点；布局的拥塞代价仅针对2.5D，3D未提及类似优化。

## 6. 主要结论与发现

1. **3D FPGA无需最先进封装技术即可实现可靠路由**：10μm微凸点（Nsg=1）下仍能路由所有基准电路；而过去认为需要1μm混合键合。
2. **3D FPGA相比2D实现线长减少14%、CPD改善6%**（在最佳配置1μm、全输出引脚连接下）；即便资源受限（10μm、Nsg=1），CPD仅有约4%提升，WL略差但面积最小。
3. **2.5D FPGA在约32%裸片间连接度下即可可靠路由**，仅比2D增加2%线长和4%CPD，但需要较长的中介层导线（≥13个逻辑块行）来避免边界拥塞。
4. **中介层导线长度的权衡**：更长导线提高连接密度但增加延迟和路由时间；最佳区域在13~17个逻辑块行（5μm间距）。
5. **扇入/扇出的影响**：增加Fg改善CPD和路由时间但增加面积开销；Fg=8~32范围内面积延迟积最优。
6. **ESD保护移除可节省面积**（最大6.29%），在连接密集架构中收益显著。

## 7. 优点

- **开源与可复现**：所有扩展（SG语法、CAD流、模型）已集成至VTR主分支，显著降低后续研究者门槛。
- **物理建模细致**：HSPICE电路仿真考虑了实际多级驱动、MUX、ESD、中介层/堆叠互连的RC，而非简单使用Elmore延迟。
- **架构描述灵活**：Scatter-Gather语法支持独立指定扇入、扇出、导线长度、方向性，适用于2.5D/3D统一描述。
- **CAD优化实用**：针对2.5D提出的解耦查找表改进使路由时间降低2.5倍；拥塞感知布局代价有效避免边界热点。
- **设计空间覆盖广**：同时探索3D和2.5D，并给出了可路由性边界（如25μm 3D失败、2.5D需要α≥0.24），工程指导价值高。

## 8. 不足与局限

- **仅考虑同质集成**：论文中3D和2.5D均为两个同构裸片（逻辑裸片），未涉及异构（如逻辑+SRAM、逻辑+HBM），真实场景中异构集成更常见。
- **单一工艺节点**：有源裸片固定为7nm FinFET，中介层固定为45nm全局金属，未评估更先进或更保守节点的异同。
- **布局算法局限**：虽然引入了拥塞代价，但3D布局未有效利用裸片间连接差异（仅通过HPWL间接鼓励）；对于长中介层导线，路由器搜索框扩展导致路由时间急剧上升（论文提及“motivating future work”）。
- **EDA runtime报告不完整**：仅给出归一化路由时间，缺乏绝对运行时间，难以评估实际大规模电路的可行性。
- **基准电路规模有限**：所用Koios子集最大759k原始元件，对于现代超大型设计（数千万LUT）尚未验证。
- **ESD移除假设的可行性**：虽然引用文献表明ESD可简化，但未在实验中实际验证移除后的电路完整性和可靠性。

（完）
