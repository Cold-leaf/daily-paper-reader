---
title: "CRIMP: Compact & Reliable DNN Inference on In-Memory Processing via Crossbar-Aligned Compression and Non-ideality Adaptation"
title_zh: "CRIMP: 通过交叉对齐压缩和非理想性适配实现紧凑可靠的存内处理DNN推理"
authors: "Shuo Huai, Hao Kong, Xiangzhong Luo, Shiqing Li, Ravi Subramaniam, Christian Makaya, Qian Lin, Weichen Liu"
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08015v1"
tags: ["query:cim"]
score: 9.0
evidence: 基于交叉阵列的存内处理，通过压缩和非理想性适配实现紧凑可靠推理
tldr: "基于交叉存储器的内存处理加速器部署深度神经网络时面临浮点不兼容、冗余参数及非理想行为三大挑战。本文提出统一训练框架CRIMP，通过重用交叉存储器中的移位单元实现缩放因子近似乘法，采用核分组剪枝和交叉存储剪枝消除数据对齐硬件，并结合运行时非理想性适应减轻器件变异影响。实验表明，量化精度损失极小，剪枝稀疏度和准确率超越现有方法，在不增加硬件开销下实现VGG-16和ResNet-56在CIFAR-10上仅2.19%和1.26%的精度下降。该工作首次将三种优化集成于单一训练流程，显著降低训练开销并提升可靠性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.08015v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1431, \"height\": 378, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08015v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1425, \"height\": 751, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08015v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1424, \"height\": 682, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08015v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1408, \"height\": 521, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08015v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1428, \"height\": 486, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08015v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1420, \"height\": 615, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08015v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1292, \"height\": 681, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08015v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1345, \"height\": 572, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.08015v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1433, \"height\": 856, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08015v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1398, \"height\": 1124, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08015v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1331, \"height\": 404, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08015v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1443, \"height\": 520, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08015v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1453, \"height\": 984, \"label\": \"Table\"}]"
motivation: 现有IMP加速器部署DNN时面临浮点运算不兼容、参数冗余导致交叉存储占用过多以及器件非理想性影响精度，且现有方案分别处理引入大量硬件开销。
method: 提出CRIMP框架，复用交叉存储移位单元近似缩放因子乘法，通过核与交叉存储两级剪枝消除数据对齐硬件，并从训练阶段引入运行时感知的非理想性适应机制。
result: "在CIFAR-10上，VGG-16精度下降仅2.19%，ResNet-56下降1.26%，均无硬件开销；剪枝稀疏度与准确率优于现有方法。"
conclusion: CRIMP框架通过单次训练同时解决量化、剪枝和鲁棒性三问题，实现高可靠、低开销的IMP部署，显著降低训练代价并提升模型实用性。
---

## 摘要
基于交叉杆的存内处理（IMP）加速器实现了深度神经网络（DNN）的高速、低功耗计算，但面临三个障碍。首先，浮点（FP）算术与交叉杆不兼容，现有量化方案仍需要FP处理器来处理缩放因子，带来硬件开销。其次，冗余的DNN参数占用过多交叉杆，当前IMP感知的剪枝方法需要跨交叉杆进行数据对齐，引入了显著的内存和计算开销。第三，非理想的交叉杆行为（如写入变化）降低了部署模型的准确性，现有补偿方法增加了大量开销。本文在一个训练过程中同时解决这三个问题。我们重用交叉杆中的位移单元来近似乘缩放因子，避免了FP处理器。我们应用核组剪枝和交叉杆剪枝来移除数据对齐所需的硬件单元。我们采用运行时感知的非理想性适配，通过利用交叉杆特性从训练阶段减轻器件非理想性的影响。将这三项优化整合到一个综合学习框架中，减少了训练开销并提高了准确性。实验表明，我们的量化导致可忽略的精度下降，并且我们的剪枝比现有方法实现了更高的稀疏性和准确性。我们的框架为IMP加速器上的CIFAR-10生成了仅整型、剪枝且可靠的VGG-16和ResNet-56模型，精度分别仅下降2.19%和1.26%，且没有硬件开销。

## Abstract
Crossbar-based In-Memory Processing (IMP) accelerators achieve high-speed, low-power computing for deep neural networks (DNNs), but face three obstacles. First, floating-point (FP) arithmetic is incompatible with crossbars, and existing quantization schemes still require FP processors for scaling factors, incurring hardware overhead. Second, redundant DNN parameters occupy too many crossbars, and current IMP-aware pruning methods require data aligning across crossbars, introducing significant memory and computing overhead. Third, non-ideal crossbar behaviors such as write variations degrade the accuracy of deployed models, and existing compensation methods add substantial overhead. In this paper, we address all three problems within a single training process. We reuse bit-shift units in crossbars to approximately multiply scaling factors, avoiding FP processors. We apply kernel-group pruning and crossbar pruning to remove the hardware units needed for data aligning. We adopt runtime-aware non-ideality adaptation to relieve the impact of device non-ideality from the training stage by exploiting crossbar features. Integrating these three optimizations into one comprehensive learning framework reduces training overhead and improves accuracy. Experiments show that our quantization incurs a negligible accuracy drop, and our pruning achieves higher sparsity and accuracy than state-of-the-art methods. Our framework produces integer-only, pruned, and reliable VGG-16 and ResNet-56 models for CIFAR-10 on IMP accelerators, with accuracy drops of only 2.19% and 1.26%, respectively, without hardware overhead.

---

## 论文详细总结（自动生成）

好的，根据您提供的论文内容，以下是该论文的详细中文总结。

## 1. 论文的核心问题与整体含义

*   **研究动机与背景**：随着深度神经网络在边缘设备上的广泛应用，其对算力和内存资源的需求日益增长。基于阻变存储器（ReRAM）的存内处理（IMP）交叉杆架构因其能够消除“冯·诺依曼瓶颈”，实现高并行、低功耗计算，成为了一种极具潜力的解决方案。然而，将DNN部署到IMP交叉杆上存在三大核心挑战：
    1.  **浮点运算不兼容**：交叉杆只能执行整数运算，而DNN的浮点参数和缩放因子需要额外的浮点处理器（如乘法器），这带来了显著的面积和功耗开销。
    2.  **模型参数冗余**：高精度的DNN模型参数众多，会占用大量交叉杆资源。现有的IMP感知剪枝方法（如列/行剪枝）虽能减少交叉杆数量，但必须引入额外的硬件单元（如稀疏表ST）来处理剪枝后的数据对齐，这反而降低了硬件集成度。
    3.  **器件非理想性**：ReRAM器件固有的非理想行为（如写入变化WV、卡滞故障SAF）会导致实际编程的电阻值与期望值偏差，从而严重影响模型推理精度。现有的补偿方法要么需要昂贵的写入验证硬件，要么会引入额外的训练、硬件或运行时开销。

*   **整体含义**：本文旨在同时解决上述三个挑战，提出一个统一、协同优化的训练框架，以生成一个既紧凑（Compact）又可靠（Reliable）的DNN模型，使其能够高效、低功耗且稳定地部署在IMP加速器上，而无需任何额外的硬件开销。

## 2. 论文提出的方法论

论文的核心方法是 **CRIMP**，一个集成了**交叉对齐剪枝**、**仅整数量化**和**运行时感知的非理想性适配**的综合学习框架。

*   **核心思想**：将剪枝、量化和非理想性适应这三个互相影响的优化过程集成到一次训练流程中，让它们相互协同，以最大程度地减少精度损失和硬件开销。

*   **关键技术细节**：
    1.  **仅整数量化**：
        *   **问题**：传统量化方法需要浮点缩放因子，需要额外的乘法器。
        *   **方案**：将训练得到的缩放因子强制近似为2的整数次幂（如公式5所示）。这样一来，缩放因子的乘法运算就可以通过交叉杆内已有的“移位加”单元（S+A）以位移操作替代，完全消除了对浮点/定点乘法器的需求。
            *   `Q(X_{L+1}) = 2^{a1+a2-b1} * { Q(X_L) ⊛ (Q(W_L) - Z_W) } + 2^{a3-b1} * (Q(B_L) - Z_B)` (其中 `Q` 为量化值，`a1, a2, a3, b1` 为整数指数)
    2.  **交叉对齐剪枝**：
        *   **问题**：细粒度剪枝需要稀疏表进行数据对齐，增加硬件开销。
        *   **方案**：提出一种多粒度剪枝方法，确保剪枝后的模型自动保持数据对齐。
            *   **核组剪枝**：移除整个核组。移除后，剩余核的数量必须是交叉杆宽度（`XB_w`）的整数倍。这确保了核维度的对齐。
            *   **交叉杆剪枝**：在训练过程中引入一个与每个交叉杆对应的可微掩码。将整个掩码值归零即意味着移除该交叉杆对应的所有权重块。这保证了交叉杆块内部的数据自然对齐。
    3.  **运行时感知的非理想性适配**：
        *   **问题**：现有方法未充分考虑IMP的推理流程和交叉杆特性（如高度）。
        *   **方案**：构建一个精确模拟DNN在IMP设备上运行时行为的“运行时模拟器”（Runtime Simulator）。
            *   **核心观察**：论文通过数学推导（公式10）和实验表明，当交叉杆高度（H）较小时，由写入变化（WV）引起的累积误差可以被ADC中的舍入函数自动抵消，从而误差期望为0。
            *   **步骤**：模拟器将量化后的8-bit权重拆分为4个2-bit值，模拟映射到ReRAM单元的过程。然后，根据写入变化的分布（`e^θ`）生成权重误差，并按照交叉杆高度分割，计算并舍入后得到最终的误差，将其加回到理想计算结果中。
            *   **作用**：这个模拟过程在训练的正向传播中被调用，使得模型能学习到对非理想性鲁棒的解，从而无需添加任何硬件即可适应器件缺陷。
    4.  **综合学习框架**：
        *   **流程**：训练分为两个阶段。首先进行**核组剪枝和量化**，然后进行**交叉杆剪枝**。在两个阶段中，都集成了**仅整数量化**和**非理想性适应**。
        *   **动态零恢复**：框架采用由“zero epoch”和“recovery epoch”交替组成的训练策略。在zero epoch执行剪枝、量化并引入非理想性影响；在recovery epoch，允许被暂时归零的掩码（核/交叉杆的重要性因子）恢复，从而让模型有机会探索更优的架构。这是通过动量优化器对过去梯度的累积实现的。

## 3. 实验设计

*   **数据集与模型**：在 **MNIST（LeNet-5）**、**CIFAR-10（VGG-16， ResNet-56）** 和 **ImageNet（ResNet-18）** 三个标准数据集和代表性DNN模型上进行了评估。
*   **硬件基准**：采用了基于 ISAAC 架构的 IMP 加速器，交叉杆设置为 **128×128**，每个存储单元存储 **2-bit**，模型量化为 **8-bit**。
*   **对比方法**：
    *   **量化方法**：与 **IAO** 对比。
    *   **剪枝方法**：与 **LSRR**, **CSAO**, **SPRC**, **XBA**, **PIM-P** 等IMP感知剪枝方法对比。
    *   **非理想性适应方法**：与 **R-V-W**, **AIIR**, **KD**, **CorrectNet**, **CTSF**, **VACTSF**, **DRD** 等方法对比（涵盖了硬件、训练和混合方案）。
*   **评价指标**：主要关注**模型准确率（Accuracy）、准确率下降（Acc. Drop）、稀疏率（Sparsity Rate）、计算功耗（Computing Power）、计算面积（Computing Area）**。

## 4. 资源与算力

论文中**未明确提及**训练所使用的GPU型号、数量和具体训练时长。仅提到了训练轮数（如CIFAR-10为160 epochs，ImageNet为90 epochs）。这使得其他研究者难以精确复现其训练成本。

## 5. 实验数量与充分性

*   **实验数量**：实验非常充分，涵盖了：
    *   量化方法的消融实验。
    *   完整模型（无非理想性）的剪枝与量化对比实验。
    *   功率与面积的评估实验。
    *   非理想性影响及适应效果的探索实验（图8，分析了交叉杆高度、剪枝和适应的影响）。
    *   最终完整框架（剪枝+量化+适应）与多种SOTA方法的全面对比实验。
*   **充分性与公平性**：
    *   **充分性**：实验覆盖了从简单到复杂的数据集（MNIST到ImageNet），多种主流网络结构，以及与多个领域内方法的对比。对比指标全面，考虑了精度和硬件效率。
    *   **客观性与公平性**：在对比中，论文努力控制了变量（如量化位宽，是否剪枝），并指出了其他方法引入的额外硬件开销（如ST、FP处理器）。然而，部分对比方法（如SPRC）的基线准确率未被报告，这为直接比较带来了一些难度。总体来说，实验设计是精心且相对公平的，有力支持了其结论。

## 6. 主要结论与发现

1.  **量化有效**：提出的仅整数量化方法（CRIMP-Quan）与IAO等需要乘法的量化方法相比，精度损失可忽略不计，证明通过训练进行“2的幂次近似”是可行的。
2.  **剪枝优异**：提出的交叉对齐剪枝方法（CRIMP-PQ）在**更高的稀疏率**下，实现了比所有现有IMP感知剪枝方法**更高或持平且不显著下降的准确率**。例如，在ID上，ResNet-18在50.50%的稀疏率下，准确率仅下降0.99%，远优于其他方法。
3.  **硬件效率提升巨大**：最终模型在计算功耗和计算面积上获得了**平均122.38倍和19.51倍的显著降低**，归功于消除了ST、FP处理器等硬件单元。
4.  **可靠性高**：在对SAF和WV同时考虑的情况下，CRIMP能够生成紧凑且可靠的模型。在CIFAR-10上，VGG-16和ResNet-56在提升稀疏度后，准确率下降仅为**2.19%和1.26%**，无需任何额外硬件开销。相比于其他有硬件开销的方法，如R-V-W、AIIR等，CRIMP在精度和成本上均具有优势。

## 7. 优点

*   **协同优化的创新性**：首次提出将**剪枝、量化和非理想性适应**这三种对IMP至关重要的技术集成到一个单一的、端到端的训练框架中，而不是分别处理，这降低了重复训练的开销，并实现了更好的协同效果。
*   **硬件感知的深度设计**：每一项技术都高度契合IMP硬件特性。如：
    *   量化利用了已有的S+A单元，实现了零额外硬件开销。
    *   剪枝通过保证数据对齐避免了昂贵的ST硬件。
    *   非理想性适应模拟了真实的器件和交叉杆推理流程，并利用了ADC的特性来消除误差。
*   **零硬件开销**：最终模型是“全整数、可剪枝、抗非理想性”的，可以直接部署在IMP加速器上，**无需添加任何ST、FP处理器或额外补偿电路**。这是该方法最突出的亮点。
*   **高效的训练策略**：提出的“动态零恢复”机制有效地解决了掩码训练和收敛困难的问题，使得模型能够探索出更高效的架构。

## 8. 不足与局限

*   **算力资源信息缺失**：论文未提及训练所需的GPU算力，这使得评估该框架在资源受限场景下的应用门槛变得困难。
*   **潜在的选择偏差**：在对比中，部分比较强的基线（如XBA, PIM-P）在ImageNet上的实验是对所有方法重新评估还是直接引用原结果？文中描述为“CRIMP-PQ”达到的指标，但未明确说明是否在完全相同的实验设置（如相同的预训练模型、随机种子）下进行公平比较。这可能会引入细微偏差。
*   **应用限制**：
    *   **假设局限**：模型假设所有输入比特 `v` 为0或1的概率相等，且权重值在量化后等概率分布。虽然简化了模型，但实际中网络权重分布可能不是均匀的，这可能会对误差期望的精确性产生影响。
    *   **模型规模局限**：实验主要验证了VGG和ResNet等中等规模的模型。该方法在更大规模（如Transformer架构或更大图像分辨率）的DNN上的效果，以及其训练框架的收敛性尚待验证。
    *   **硬件依赖性**：其结论（如最佳交叉杆高度）是建立在特定平台（ISAAC，128x128 crossbar）和特定的非理想性模型（log-normal分布的WV）之上的。对于不同规格的交叉杆或更复杂的非理想性模型，方法的通用性和优化结论可能需要重新验证。
*   **长期可靠性评估**：论文仅关注了部署时的非理想性影响，未评估ReRAM器件的**寿命相关退化**（如擦写循环导致的磨损）对模型长期稳定性的影响。文中提到的“减少操作次数以延长寿命”的动机，在最终方法中并未直接量化评估。

（完）
