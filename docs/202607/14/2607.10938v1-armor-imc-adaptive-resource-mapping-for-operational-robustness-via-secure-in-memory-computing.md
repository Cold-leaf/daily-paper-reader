---
title: "ARMOR-IMC: Adaptive Resource Mapping for Operational Robustness via Secure In-Memory Computing"
title_zh: ARMOR-IMC：通过安全存内计算实现操作鲁棒性的自适应资源映射
authors: "Muhtasim Alam Chowdhury, Ramtin Zand, Soheil Salehi"
date: 2026-07-12
pdf: "https://arxiv.org/pdf/2607.10938v1"
tags: ["query:cim"]
score: 9.0
evidence: 针对存内计算加速器的安全与可靠性加固
tldr: "传统架构中数据移动开销大，基于SOT-MTJ的模拟存内计算（IMC）虽能高效处理DNN，但面临工艺变化（PV）和侧信道攻击（SCA）的双重威胁。现有防御孤立处理两者，本文提出ARMOR-IMC后训练框架，利用变异影响得分（VIS）指导故障观察窗口映射，并引入单次推理泄漏（LPI）指标量化功耗变化。实验表明，PV导致的精度下降超50%被恢复至近基线，同时有效缓解基于相关性的功耗分析攻击，实现鲁棒性与安全性的协同增强。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.10938v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 878, \"height\": 357, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10938v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 654, \"height\": 372, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10938v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 923, \"height\": 537, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10938v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 919, \"height\": 316, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10938v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 599, \"height\": 468, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.10938v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 907, \"height\": 219, \"label\": \"Table\"}]"
motivation: 模拟IMC加速器同时面临制造工艺变化导致可靠性下降和功耗侧信道攻击威胁，现有防御方案孤立处理单一问题。
method: 提出后训练框架，基于变异影响得分（VIS）引导故障观察窗口（FOW）映射，并引入单次推理泄漏（LPI）度量输入相关功耗变化。
result: "恢复因工艺变化导致的下滑超50%的精度至近基线水平，并显著降低相关性功耗分析攻击的成功率。"
conclusion: 该框架无需重训练即可同时增强模拟IMC加速器对工艺变化和侧信道攻击的鲁棒性，实现高可靠性安全推理。
---

## 摘要
传统架构中巨大的数据移动开销导致了采用存内计算（IMC）进行能效深度神经网络（DNN）处理。通过利用自旋轨道扭矩磁隧道结（SOT-MTJ）等新兴器件，IMC绕过了“存储墙”并降低了传统CMOS固有的漏电功耗。然而，这种转变引入了双重硬件威胁：制造工艺变化（PV）降低了可靠性，增加了对故障注入的脆弱性，而电源侧信道攻击（SCA）则危及安全性。现有防御措施孤立地应对这些威胁。本文提出了一种训练后框架，该框架同时强化模拟IMC加速器抵御这两种威胁，而无需重新训练模型。该框架在IMAC-Sim模拟器中实现，使用所提出的变化影响分数（VIS）来指导故障观察窗口（FOW）的映射，并引入每次推理泄漏（LPI）指标来量化随机注入下与输入相关的功耗变异性以及由此导致的有效信噪比降低。实验表明，PV引起的故障可使精度下降超过50%，而我们的方法恢复了接近基线的精度，并减轻了基于相关性的功耗分析攻击的威胁。

## Abstract
The massive data-movement overhead in traditional architectures has led to the adoption of In-Memory Computing (IMC) for energy-efficient Deep Neural Network (DNN) processing. By leveraging emerging devices like Spin-Orbit Torque Magnetic Tunnel Junctions (SOT-MTJs), IMC bypasses the "memory wall" and reduces leakage power inherent in traditional CMOS. However, this shift introduces dual hardware threats: manufacturing Process Variation (PV) degrades reliability and increases vulnerability to fault injection, while power Side-Channel Attacks (SCAs) compromise security. Existing defenses address these threats in isolation. This work presents a posttraining framework that simultaneously hardens analog IMC accelerators against both threats without retraining the model. Implemented in the IMAC-Sim simulator, our approach uses the proposed Variation Impact Score (VIS) to guide the mapping of Fault Observation Windows (FOWs) and introduces the Leakage Per Inference (LPI) metric to quantify input-dependent power variability under stochastic injection and the resulting reduction in effective signal-to-noise ratio. Experiments show that PV-induced faults can degrade accuracy by over 50%, while our method restores near-baseline accuracy and mitigates the threat of correlation-based power analysis attacks.

---

## 论文详细总结（自动生成）

### 论文核心问题与整体含义（研究动机和背景）

- **研究动机**：传统冯·诺依曼架构在深度神经网络（DNN）推理中存在严重的数据移动开销，而基于自旋轨道扭矩磁隧道结（SOT-MTJ）的模拟存内计算（IMC）架构能够显著提升能效、绕过“存储墙”。然而，这种新兴架构面临**双重硬件威胁**：
  1. **制造工艺变化（Process Variation, PV）**：导致SOT-MTJ器件电阻状态偏移，引发系统性位翻转，严重降低推理精度（实验显示精度下降可超过50%）。
  2. **电源侧信道攻击（Power Side-Channel Attack, SCA）**：攻击者通过测量功耗推测模型参数或输入数据，威胁知识产权（IP）和隐私。
- **现有不足**：已有防御方案分别针对可靠性或安全性进行加固，但**无法同时应对两者**，系统在解决一个威胁后仍对另一个威胁脆弱。
- **整体目标**：提出一个**无需模型重训练**的后训练框架ARMOR-IMC，同时增强模拟IMC加速器对PV故障和SCA攻击的鲁棒性。

### 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：结合架构级映射策略与随机功率注入机制，通过两个互补指标（VIS和LPI）分别量化可靠性和安全性风险，并据此进行自适应资源映射。
- **关键技术细节**：
  1. **Fault Observation Windows (FOWs)**：将每个神经网络层按固定尺寸（本文采用32×32）子阵列分区，作为局部故障分析的基本单元。计算公式：  
     - 水平分区数 \( H_p = \lceil N_{out} / X_{row} \rceil \)  
     - 垂直分区数 \( V_p = \lceil (N_{in}+1) / X_{col} \rceil \)  
     例如第一层（400×120）分成 \(4\times13=52\)个FOW。
  2. **Variation Impact Score (VIS)**：对每个FOW单独注入高故障率（50%–90%）位翻转，测量推理精度下降程度，从而识别最脆弱的FOW。VIS值形成热力图，指导权重重映射。
  3. **动态权重映射策略**：
     - 计算每个FOW的**平均绝对误差（MAE）**（公式2），选出偏差最大的FOW \( b^* \)。
     - 从同层兼容几何尺寸的FOW中，选择基于VIS最高故障压力推理精度的“韧性伙伴”FOW \( k^* \)；若精度相同，则选平均功耗最低者。
     - 交换 \( S_{b^*} \) 和 \( S_{k^*} \) 的内容，将重要权重映射到更可靠的物理区域，无需修改交叉开关结构。
  4. **Leakage Per Inference (LPI) 指标**：定义为多次推理下平均功耗的标准差，作为输入相关功耗变异性的量化指标。基线分析显示输入数字类别之间存在显著差异（如数字0平均4.60W，数字6平均4.68W），表明存在可被CPA利用的泄漏。
  5. **确定性/随机噪声注入**：
     - 确定性功率平衡：为每个输入添加补偿电流，使总功率恒定，但留下静态签名易被平均攻击破解。
     - 随机化策略：在每个推理窗口，从均匀分布 \(U(\Delta P_{min},\Delta P_{max})\) 中抽取辅助功耗 \(P_{aux}(i,t)\)，通过时间门控电阻负载实现，使总功耗 \(P_{obs}(i,t)=P_{base}(i,t)+P_{aux}(i,t)\) 变为非平稳随机过程，防止攻击者建立稳定关联。

### 实验设计：数据集、场景、基准、对比方法

- **数据集**：MNIST手写数字识别（10类）。
- **网络架构**：3层全连接DNN，结构为400×120×84×10，对应权重数量：层1（48,000）、层2（10,080）、层3（840）。
- **模拟平台**：IMAC-Sim（基于Python的电路级模拟器，生成SPICE网表，可建模互连寄生和物理布局约束）。SOT-MTJ器件电阻采用低阻态5KΩ、高阻态15KΩ。
- **基准 (Baseline)**：无故障时的推理精度（69%）和平均功耗（4.7W）。
- **对比方法**：
  - 无映射（Without mapping）：直接保留故障位翻转。
  - VIS引导映射（With mapping）：应用所提动态权重映射。
  - 确定性功率平衡（作为主动防御基线）。
  - 随机噪声注入（最终方案）。
- **实验场景**：
  - 均匀位翻转率从5%至25%（整体精度退化趋势图）。
  - 局部FOW级故障注入率50%–90%（评估VIS热力图和映射效果）。
  - 输入相关功耗泄露分析：对每个数字类别（0–9）测量400次推理的功率统计。
  - 随机噪声注入效果：注入范围[0.2, 0.4]W，观察信号重叠。

### 资源与算力

- 文中**未明确说明**使用的GPU型号、数量或训练时长。所有实验均基于IMAC-Sim电路级模拟器进行，可能主要依赖CPU运行蒙特卡洛模拟（100次/配置），未涉及GPU训练（因为采用后训练框架，无模型重训练）。
- **需指出**：论文未提供算力资源细节，实验可重现性依赖于IMAC-Sim模拟环境而非特定硬件。

### 实验数量与充分性

- **实验数量**：
  - 整体精度退化实验：5个均匀故障率点（5%–25%），每个点100次蒙特卡洛运行。
  - VIS局部故障分析：3个层×5个故障率（50%–90%），每个FOW独立注入，共（52+12+3）×5=335次独立注入实验，每次推理100张图像。
  - 功耗基线测量：10个数字类别×400次推理=4000次测量。
  - 映射有效性比较：3个层×5个故障率×2种策略（有/无映射）=30组对比。
  - 随机噪声注入实验：展示分布重叠示例（Layer1和Layer2）。
- **充分性评估**：
  - 优点：覆盖了不同故障率、不同层、不同FOW粒度；同时评估了可靠性与安全性两个维度；使用了蒙特卡洛模拟增强统计可靠性。
  - 不足：仅在MNIST单数据集、小规模DNN上测试，未在ImageNet等复杂任务或更深网络（如ResNet）验证；未与传统防御方法（如纠错码、掩码）进行定量对比；未评估实际CPA攻击成功率，仅用LPI和信号重叠作为间接指标。

### 论文的主要结论与发现

1. **VIS引导映射显著恢复PV导致的精度损失**：在90%局部故障率下，层3精度从17%提升至34%（接近翻倍），层1恢复至接近基线（62%→68%）。层2在高故障率下也有效，但在50%故障率时出现轻微下降（因边缘FOW尺寸不兼容导致次优映射）。
2. **确定性功率平衡可压制LPI（降低>96%），但留下静态签名**，易被平均攻击破解。
3. **随机噪声注入使功率分布在数字类之间高度重叠**（如Layer1重叠约34mW，总噪声带宽180mW超过基线泄漏信号140mW），有效降低有效信噪比（SNR < 1），使攻击者难以区分输入类别。
4. **功耗开销**：随机注入平均额外功耗约0.30W（约6.5%），但非恒定运行模式（时间门控）增加了攻击难度。

### 优点：方法或实验设计上的亮点

- **方法创新**：
  - 首个同时针对PV故障和SCA攻击进行协同加固的后训练框架，无需重训练模型。
  - VIS指标结合FOW粒度实现细粒度脆弱性定位，映射策略简单有效。
  - LPI作为安全性代理指标，无需执行完整CPA即可量化泄漏风险。
  - 随机噪声注入采用移动目标防御思想，使功耗轨迹非平稳，从根本上破坏统计关联性。
- **实验设计**：
  - 使用电路级模拟器（IMAC-Sim）提供真实SPICE级别的功耗和延迟评估。
  - 故障注入覆盖广泛范围（5%–90%），并区分均匀与局部场景。
  - 对每个数字类别进行多次采样（400次），统计结果可靠。

### 不足与局限

- **实验覆盖有限**：
  - 仅在MNIST单数据集和浅层全连接网络上测试，未在CIFAR、ImageNet等复杂图像任务上验证，也未评估卷积神经网络或更深结构。
  - 仅模拟了SOT-MTJ的工艺变化，未考虑其他新兴器件（如RRAM、PCM）或温度、电压波动等次要因素。
- **安全评估不完整**：
  - 未实施实际CPA/DPA攻击并测量成功率，仅用LPI和信号重叠作为间接证据，说服力有限。
  - 随机噪声注入可能被高级攻击（如机器学习侧信道）通过去噪技术部分恢复，文中未讨论应对策略。
- **映射策略的局限性**：
  - 层2在50%故障率时出现精度下降，源于严格尺寸兼容性约束导致次优伙伴选择。若扩大候选池或引入部分重新配置可改善。
  - 映射仅在层内进行，未跨层迁移权重。
- **功耗开销未优化**：随机注入功耗约6.5%的额外开销，且未评估延迟影响（文中未提及延迟变化）。
- **可复现性**：未提供代码或详细参数（如SOT-MTJ模型参数、随机噪声分布范围选择的依据），第三方难以复现。

（完）
