---
title: Generative Design of Liquid-Cooling Channels for Thermal Management of 2.5D and 3D Integrated Advanced Packaging
title_zh: 用于2.5D和3D集成先进封装热管理的液冷通道生成式设计
authors: "Michael Acquah, Zheng Liu"
date: 2026-08-24
pdf: "https://arxiv.org/pdf/2608.22787v1"
tags: ["query:thermal-gnn"]
score: 8.0
evidence: 利用生成式设计优化2.5D/3D先进封装液冷通道，直接应对芯片散热问题。
tldr: "面向2.7kW多芯片封装（双GPU+CPU）的散热需求，现有冷却通道设计依赖经验且成本高。本文提出物理引导的生成式设计框架，以条件扩散模型生成对称液冷通道拓扑，并联合连通性筛查与校准的降阶热液模型评估。从5000个布局中筛出最优设计G1016，预测最高温度70.3°C，较传统参考最高温度、温差、压降分别降低33.6%、52.5%、72.8%。该方法将全阶CFD仅用于最终验证，高效发掘非传统冷却结构，为先进封装热管理提供新范式。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.22787v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1160, \"height\": 1818, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.22787v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1585, \"height\": 1050, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.22787v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1700, \"height\": 1471, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.22787v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1412, \"height\": 696, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.22787v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1635, \"height\": 598, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.22787v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1010, \"height\": 594, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.22787v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1714, \"height\": 302, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.22787v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1712, \"height\": 396, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.22787v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1713, \"height\": 293, \"label\": \"Table\"}]"
motivation: 先进封装功率密度激增，现有液冷通道设计依赖经验与试错，难以满足高效散热需求。
method: 提出物理引导生成式设计框架，利用条件扩散模型生成对称通道布局，经连通性与死端分支筛查后，用降阶热液模型评估性能。
result: "生成5000个布局，2220个连通，229个通过筛选，最优设计G1016的预测最高温度70.3°C、温差24.9°C、压降89.72kPa，较传统分别下降33.6%、52.5%、72.8%。"
conclusion: 物理引导生成式设计能以极低CFD成本发现非传统冷却架构，并满足降阶模型与全阶仿真验证，为先进封装热管理提供高效新范式。
---

## 摘要
高功率多芯片封装在有限的封装面积内产生更多热量，因此需要日益有效的冷却。本文提出了一种物理引导的生成式设计框架，用于一个包含两个高功率GPU和一个CPU的2.7千瓦多芯片封装中的液冷通道拓扑优化。条件扩散模型以最大GPU温度、GPU温度分布和压降作为性能目标生成对称通道布局。生成的設計经过连通性和死端分支筛选，然后使用校准的降阶热-水力模型进行评估。在生成的5000个布局中，2220个包含连续的入口到出口流动路径，229个满足最终的拓扑筛选标准。多目标分析确定G1016为热性能最优的可行设计，其预测最大GPU温度为70.30°C，GPU温度分布为24.90°C，压降为89.72 kPa。与传统参考拓扑相比，G1016将最大GPU温度、温度分布和压降分别降低了33.6%、52.5%和72.8%。在OpenFOAM中进行的独立三维共轭传热仿真预测最大GPU温度为66.70°C，压降为92.1 kPa，与降阶模型在温升方面的差异约为8.6%，在压降方面的差异约为2.6%。结果表明，物理引导的生成式设计可以高效地发现非传统的冷却通道架构，同时将昂贵的全阶CFD限制在最终验证阶段。

## Abstract
High power multi-chip packages require increasingly effective cooling as more heat is generated within a limited package area. This work presents a physics-guided generative design framework for liquid cooling channel topology optimization in a 2.7 kW multi-chip package containing two high power GPUs and one CPU. A conditional diffusion model generates symmetric channel layouts using maximum GPU temperature, GPU temperature spread, and pressure drop as performance targets. Generated designs are subjected to connectivity and dead-end-branch screening and are then evaluated using a calibrated reduced-order thermal-hydraulic model. Of 5,000 generated layouts, 2,220 contained a continuous inlet-to-outlet flow path and 229 satisfied the final topology-screening criteria. Multi-objective analysis identified G1016 as the thermally leading feasible design, with a predicted maximum GPU temperature of 70.30 °C, GPU temperature spread of 24.90 °C, and pressure drop of 89.72 kPa. Compared to a conventional reference topology, G1016 reduced the maximum GPU temperature, the temperature spread, and the pressure drop by 33.6%, 52.5%, and 72.8%, respectively. Independent three-dimensional conjugate heat-transfer simulation in OpenFOAM predicted a maximum GPU temperature of 66.70 °C and a pressure drop of 92.1 kPa, corresponding to ROM differences of approximately 8.6% in temperature rise and 2.6% in pressure drop. The results demonstrate that physics-guided generative design can efficiently discover non-conventional cooling channel architectures while limiting expensive full-order CFD to final validation.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：高功率多芯片先进封装（2.5D/3D集成）在有限封装面积内产生极高热流密度，传统风冷受限于热容量，而液冷通道的设计面临散热能力与压降损失之间的根本性权衡。如何在复杂的多芯片布局下高效、系统地设计出热性能和流体性能俱佳的液冷通道拓扑，是本文要解决的关键问题。
- **研究动机**：
  - 多芯片封装包含不同功耗、尺寸和位置的发热元件，造成非均匀热分布和局部热点，简单的直通道冷板无法有效地将冷却液分配到所有芯片区域。
  - 手动设计复杂通道布局（如分叉、歧管、多通道网络）难度极大，设计空间大、目标互相制约（最高温度、温度均匀性、压降、泵功率）。
  - 传统拓扑优化方法虽然系统，但计算成本高（需反复求解流动和能量方程），且生成的连续拓扑常需后处理才能变为可制造的连通通道。
  - 机器学习与生成式模型（特别是扩散模型）为加速通道布局设计提供了新的可能性，但仍需满足连通性、最小通道宽度、对称性、芯片区域覆盖等物理和几何约束。
- **整体含义**：本文旨在提出一个“物理引导的生成式设计框架”，以极低的CFD开销高效发现非传统的冷却通道架构，同时通过全阶仿真验证其性能，为先进封装热管理提供一种新的高效设计范式。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

### 2.1 总体框架
本文提出的框架由五个核心环节组成：
1. **条件扩散模型**生成对称通道布局；
2. **拓扑筛选**（连通性与死端分支过滤）去除不可行设计；
3. **校准的降阶热-水力模型（ROM）** 快速评估所有可行设计的性能；
4. **多目标分析**（Pareto支配）识别高性能候选设计；
5. **全阶 OpenFOAM 三维共轭传热（CHT）仿真**对最终选定设计进行独立验证。

### 2.2 降阶热-水力模型（ROM）
- **应用场景**：2.7 kW 多芯片封装，含两个 1.2 kW GPU 和一个 300 W CPU，矩形域 189.5 mm × 270.4 mm，以 200×200 笛卡尔网格离散。
- **等效固体传导模型**：将 4 mm 铜层和 2 mm 硅层等效为单层，等效面内导热系数 k_eff = 316.7 W m⁻¹K⁻¹，总厚度 t_eff = 6 mm；在芯片区域附加穿透厚度热阻（R_stack = 2.33×10⁻⁵ m²K W⁻¹）来计算结温。
- **深度平均流动模型**：仅在连通的流体区域内求解拉普拉斯方程 ∇²p* = 0，得到压力势场和速度场，并通过缩放严格满足 5 L min⁻¹ 的流量约束。
- **局部对流换热**：由欧几里得壁距变换估计局部通道宽度和水利直径，计算局部雷诺数；层流区采用 Nusselt=5.39，湍流区采用 Gnielinski 关联式，Re=2000~4000 之间线性过渡；热传递系数乘以校准系数 C_h=2.6841。
- **路径相关冷却剂升温**：根据最短路径距离定义归一化路径进度 s(x,y)，冷却剂温度沿通道线性升温，与固体传热方程迭代耦合（4次外迭代）。
- **水力压降模型**：按流道排计算摩擦损失（层流 f=64/Re，湍流 f=0.316Re⁻⁰·²⁵），加入通道蜿蜒度修正τ，并累加逐排的突然扩缩损失，最后乘以校准系数 C_p=1.89。
- **校准**：基于代表性通道布局的CFD结果，热系数 C_h 的拟合使温度升平均误差约 7.7%、最大误差约 12.6%；校准后两个系数对所有后续拓扑固定不变。

### 2.3 条件扩散模型
- **数据表示**：通道布局以 200×200 二进制掩膜表示，训练时压缩为 200×100 半域图像，通过关于中心线的镜像重构完整对称布局。
- **条件向量**：c = [T_max,GPU（归一化）, ΔT_GPU（归一化）, log(ΔP)（归一化）]，即三个设计目标作为生成条件。
- **模型结构**：条件去噪 U-Net 预测噪声 ε_θ(x_t, t, c)，扩散时间步与性能条件嵌入残差块中。
- **加权损失**：损失函数中引入基于死端分支比的样本权重 w_i，低分支拓扑权重更大（最多1.0），高分支拓扑最小权重视为0.25，从而优先学习干净的通流结构。
- **分类器自由引导**：训练时随机丢弃条件（比例部分更新），采样时采用无分类器引导，引导尺度 s_cfg=2.0。
- **DDIM采样**：200步扩散过程用50步DDIM确定性采样，减少计算开销，且可复现。
- **拓扑筛选**：连通性分析 + 死端分支比 F_branch = L_dead/L_skel ≤ 0.05 的阈值筛选。

### 2.4 全阶CHT模型
- OpenFOAM 多区域有限体积法，含流体、铜冷板、发热器件三个耦合区域。
- 湍流采用 RANS k–ω SST 模型；固体区域为各向同性导热；流-固界面强制温度和热流连续性。
- 边界条件与ROM一致：5 L min⁻¹、入口25°C、总热负荷2700 W、绝热外壁。

## 3. 实验设计：数据集、场景、基准与对比方法

- **数据集**：物理引导的随机几何构造生成 5,000 个初始通道布局，经过预处理（连通性、特征尺寸、芯片覆盖、死端分支比）后保留 3,213 个布局用于条件扩散模型训练（2,909 训练 / 304 验证）。
- **生成规模**：从训练好的扩散模型采样 5,000 个全新全域布局，用于评估框架。
- **测试场景**：统一的2.7 kW多芯片封装（两个1.2kW GPU + 一个300W CPU），固定流量5 L min⁻¹、入口温度25°C、通道深度1 mm。
- **基准**：传统直通道参考设计 S1（相同负载和工况）。
- **对比方法**：
  - ROM 预测 vs. OpenFOAM 三维CHT仿真（用于代表性布局 V1–V4 的ROM验证）；
  - 最终选定设计 G1016 的 ROM vs. CFD 对比；
  - 生成设计 G1016 vs. 传统参考设计 S1 的性能对比。
- **评估指标**：T_max,GPU（最大GPU温度）、ΔT_GPU（GPU温度分布）、ΔP（压降）。

## 4. 资源与算力

- **论文未明确报告算力信息**：未说明使用的 GPU 型号、数量、训练时长、DDIM采样耗时、ROM评估耗时或OpenFOAM单次仿真成本。
- 论文仅在方法论层面体现计算效率优势（ROM用于大规模筛选、全阶CFD仅用于最终验证），但未给出具体的计算资源消耗数据或加速倍数。

## 5. 实验数量与充分性

### 实验数量
- **数据生成**：初始5,000个布局 → 预处理后3,213个用于训练。
- **生成与筛选**：5,000个生成布局 → 2,220个连通 → 229个通过最终筛选（保留率4.58%）。
- **性能评估**：229个可行设计全部经ROM评估。
- **多目标分析**：识别Pareto前沿，选定G1016。
- **ROM验证**：代表性拓扑V1–V4（4组案例）。
- **最终验证**：G1016的ROM vs. OpenFOAM完整三维CHT对比（1组案例）。

### 充分性评估
- **优点**：从5000个候选到229个可行再到最终选定的筛选流程规模可观，主结论（G1016优于S1）有ROM和CFD两个层面的证据支撑，对比条件一致（相同负载、工况、材料），公平性较好。
- **不足**：
  - ROM验证案例仅4个（V1–V4）且文中未充分展示其多样性；
  - 最终CFD验证仅1个设计，样本量偏少；
  - 没有进行消融实验（如去掉连通性筛选、去掉条件引导、不使用转移学习）来量化各模块的单独贡献；
  - 没有与其他生成方法（如GAN、VAE）或传统拓扑优化方法进行直接对比，无法说明扩散模型相对于替代方案的优势幅度。

## 6. 论文的主要结论与发现

- **核心结果**：生成式设计框架能以极低的CFD成本发现非传统冷却通道架构。
- **筛选统计**：5,000个生成布局中，2,220个（44.4%）连通，仅229个（4.58%）通过全部拓扑筛选——说明扩散模型生成通道类几何能力较强，但直接保证流体连通性仍然困难。
- **最优设计 G1016**：T_max,GPU = 70.30°C，ΔT_GPU = 24.90°C，ΔP = 89.72 kPa。
- **相对传统参考 S1 的提升**：
  - 最大GPU温度降低 33.6%（105.89°C → 70.30°C）；
  - GPU温度分布降低 52.5%（52.40°C → 24.90°C）；
  - 压降降低 72.8%（329.40 kPa → 89.72 kPa）。
  - 论文指出，这种同时改善热性能和压降的情况是相对固定基线的结果，整体设计空间仍存在固有的多目标权衡。
- **CFD独立验证**：OpenFOAM预测 G1016 的 T_max,GPU = 66.70°C、ΔP = 92.1 kPa；与ROM相比，温升误差约 +8.6%、压降误差约 -2.6%，主目标指标一致性良好；但 ΔT_GPU 的误差较大（约 -16.1%），说明ROM对温度均匀性这类局部细节指标的解析能力有限。
- **总体判断**：物理引导的生成式设计可高效探索设计空间，将昂贵的全阶仿真压缩到最终验证阶段。

## 7. 优点

- **新颖的框架整合**：将条件扩散生成、拓扑筛选、ROM快速评估、Pareto多目标分析、全阶CFD验证串联为一个完整的、可复用的设计流水线，各环节分工明确。
- **物理引导贯穿全程**：DS数据构造考虑芯片覆盖和连通性、损失函数对低死端分支样本加权、生成约束为左右对称、屏幕后连通性筛选，物理约束融入多个层面。
- **降阶模型的设计合理**：2.5D公式兼顾面内热扩散、深度平均流动、路径冷却剂升温、局部传热系数变化和1D压降模型，校准后的ROM在最高温度和压降两个主指标上与CFD误差控制在可接受范围内。
- **多目标决策不引入主观权重**：基于Pareto支配分析呈现热-液权衡，最终选择优先最低峰值温度并结合温度均匀性和压降，避免简单加权和随意性。
- **验证策略务实**：ROM用于大规模筛选，全阶CFD仅用于最终选定设计的验证，显著降低计算成本，同时保留了完整三维仿真的可信度。
- **结果说服力强**：生成设计相对传统参考在热和压降上同时大幅改善（33.6%/52.5%/72.8%），且CFD独立验证支持ROM的基本结论。

## 8. 不足与局限

- **可行性率低**：仅4.58%的生成布局通过拓扑筛选，说明扩散模型本身并未内嵌连通性和低死端分支约束，依赖事后确定性筛选，大量生成计算被浪费。论文也承认这是主要局限，并指出将连通性约束融入反向采样过程是未来方向。
- **ROM精度局限**：ΔT_GPU 误差约 -16.1%，表明ROM对温度均匀性的预测不够准确；深度平均流动和等效面内固体传导无法捕捉局部回流、穿厚度热扩散等三维现象。
- **校准依赖**：C_h 和 C_p 基于特定流量、通道深度、材料组合校准，对显著不同的工况或封装布局需要重新校准和验证。
- **设计空间受限**：优化的是二维平面内的拓扑，不包括通道深度、截面形状或三维歧管分布的联合优化；单流体（水）、单相、定物性的假设也限制了实际应用的完全覆盖。
- **缺乏物理实验验证**：只有CFD仿真验证，未进行实验样件制造和测试，实际制造可行性、密封性、长期可靠性等尚未解决。
- **未与其他方法对比**：没有与GAN/VAE等生成方法或SIMP等经典拓扑优化方法在生成质量、计算代价、最终性能上进行量化比较，难以定位该方法的相对优势。
- **算力与时间成本未报告**：训练和采样计算成本不透明，影响他人复现和工程可行性评估。

## （完）
