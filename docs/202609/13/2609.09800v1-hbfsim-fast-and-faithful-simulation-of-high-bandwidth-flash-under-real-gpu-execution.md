---
title: "HBFSim: Fast and Faithful Simulation of High-Bandwidth Flash Under Real GPU Execution"
title_zh: HBFSim：在真实 GPU 执行下对高带宽闪存进行快速且忠实的仿真
authors: "Yanpeng Hu, Yiwei Yang, Yuanwu Zhu, Yusheng Zheng, Wei Zhang, Andi Quinn"
date: 2026-09-09
pdf: "https://arxiv.org/pdf/2609.09800v1"
tags: ["query:thermal-gnn"]
score: 6.0
evidence: 封装内三维堆叠闪存的含热效应仿真
tldr: 大模型推理受内存容量限制，HBF（高带宽闪存）将 NAND 堆叠进加速器封装，但硬件 2027 年才送样，容量与数据放置决策无法等待。HBFSim 通过改写 PTX 并门控 kernel 启动，让真实 GPU 执行真实负载的同时叠加 HBF 的时序、容量与热效应。它在六个标定点与实测设备完全一致，零不安全启动，vLLM 0.15.1 服务 Qwen3-30B-A3B 输出与基线一致，快路径比详细路径快 20.8 倍。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.09800v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1776, \"height\": 901, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.09800v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 866, \"height\": 602, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.09800v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1851, \"height\": 375, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.09800v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 520, \"height\": 382, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.09800v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 797, \"height\": 339, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.09800v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 780, \"height\": 681, \"label\": \"Table\"}]"
motivation: HBF 2027 年初才送样，现有存储模拟器不执行真实负载、GPU 模拟器不跑真实算子、周期级模拟器跑不完一次推理，容量与放置决策缺少依据。
method: HBFSim 改写 NVIDIA 编译器输出的 PTX 并门控 kernel 启动，分离发射与消费，时序取自实测设备，结温决定 HBF 速率与刷新期限。
result: 六个标定点与实测设备完全一致，零不安全启动；vLLM 0.15.1 服务 Qwen3-30B-A3B 返回与基线相同的 token，快路径 2s 对 44s，快 20.8 倍。
conclusion: 在 HBF 芯片送样前，HBFSim 让设计者在真实负载下实测容量与数据放置决策，而非依赖假设。
---

## 摘要
服务大语言模型（LLM）受限于内存容量。高带宽闪存（HBF）将 NAND 闪存堆叠在加速器封装内，位于高带宽内存（HBM）之下的一层；其规范于 2026 年 8 月 3 日发布，首批推理器件预计于 2027 年初提供样片。关于容量和数据放置的决策不能等待硅片。现有方法都无法定夺这些决策：重放记录访问序列的存储模拟器从不执行工作负载，GPU 模拟器不运行真实计算内核，而周期精确模拟器无法完成一次 LLM 推理运行。我们提出 HBFSim，这是首个在真实 GPU 上执行真实推理工作负载的同时，将 HBF 的时序、容量和热效应应用于该工作负载的评估平台。HBFSim 重写 PTX（NVIDIA 编译器生成的中间代码），并对内核启动进行门控；发射与消费相分离，因此由真实硬件提供计算来隐藏访问。时序来自对真实器件的测量，而非参数表；结温既决定 HBF 可持续的速率，也决定强制刷新写入的保持期限。HBFSim 在所有六个校准断点处与实测器件完全匹配，且无不安全启动；未修改的 vLLM 0.15.1 服务 Qwen3-30B-A3B 时返回与未插桩基线相同的 token 标识符。器件快速路径在 2 秒内服务同一 Qwen3-30B-A3B 案例，而详细参考路径需 44 秒，快 20.8 倍。在 HBF 器件提供样片之前，HBFSim 让设计者能够在真实工作负载下衡量容量或放置决策，而不是对其做出假设。

## Abstract
Serving a large language model (LLM) is limited by memory capacity. High-Bandwidth Flash (HBF) stacks NAND flash inside the accelerator package, one tier below high-bandwidth memory (HBM); the specification was published on August 3, 2026, and the first inference devices are expected to sample in early 2027. Decisions about capacity and data placement cannot wait for silicon. No existing method settles those decisions: a storage simulator replaying a recorded access sequence never executes the workload, a GPU simulator does not run the real compute kernels, and a cycle-accurate simulator cannot finish one LLM inference run. We present HBFSim, the first evaluation platform that applies HBF timing, capacity, and thermal effects to a real inference workload while that workload executes on a real GPU. HBFSim rewrites PTX, the intermediate code NVIDIA's compiler emits, and gates kernel launches; issue is separated from consumption, so real hardware supplies the computation that hides an access. Timing comes from measurements of a real device rather than a parameter sheet, and junction temperature sets both the rate HBF sustains and the retention deadline that forces refresh writes. HBFSim matches the measured device exactly at all six calibration breakpoints, with zero unsafe launches, and an unmodified vLLM 0.15.1 serving Qwen3-30B-A3B returns the token identifiers of the uninstrumented baseline. The device fast path serves the same Qwen3-30B-A3B case in 2s against 44s on the detailed reference path, 20.8x faster. Before HBF parts sample, HBFSim lets a designer measure a capacity or placement decision under a real workload instead of assuming one.